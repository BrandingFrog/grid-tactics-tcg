# GitHub automation

`workflows/` contains scheduled or push-triggered automation for card-of-the-day publishing, drift checks, and wiki backup. Review workflow permissions and secrets before changing these files.
