# Claude project configuration

Tracked files in this folder define repository-specific Claude hooks and settings. `settings.local.json` is machine-local and ignored.

The hooks protect sensitive card-definition edits and enforce selected terminology rules. Merge changes carefully rather than replacing the settings file wholesale.
