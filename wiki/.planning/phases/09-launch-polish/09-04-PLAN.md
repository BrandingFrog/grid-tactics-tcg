---
phase: 09-launch-polish
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Wiki is reachable at a custom domain with HTTPS (or user has decided to defer this)"
    - "All Phase 9 success criteria have been verified against the live wiki"
  artifacts: []
  key_links: []
---

<objective>
Configure custom domain (user-dependent) and run final verification of all Phase 9 success criteria.

Purpose: Complete the last launch-polish item (DEPLOY-06) and verify all Phase 9 work is live and correct.
Output: Custom domain configured (if user has one) and final verification report.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@wiki/.planning/PROJECT.md
@wiki/.planning/ROADMAP.md
@wiki/.planning/STATE.md
@wiki/.planning/phases/09-launch-polish/09-01-SUMMARY.md
@wiki/.planning/phases/09-launch-polish/09-02-SUMMARY.md
@wiki/.planning/phases/09-launch-polish/09-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Custom domain configuration for the wiki</decision>
  <context>
    SC1 requires the wiki to be reachable at a custom domain with HTTPS. This requires:
    1. A domain you own (e.g., wiki.grid-tactics.dev, wiki.gridtactics.com, etc.)
    2. DNS configuration (CNAME record pointing to Railway)
    3. Railway custom domain setup (Settings -> Networking -> Custom Domain)

    The wiki is currently live at: https://mediawiki-production-7169.up.railway.app

    Railway handles HTTPS certificates automatically for custom domains.
  </context>
  <options>
    <option id="configure-domain">
      <name>Configure custom domain now</name>
      <pros>Wiki gets a clean, memorable URL. HTTPS auto-provisioned by Railway.</pros>
      <cons>Requires DNS access and a domain you own.</cons>
    </option>
    <option id="defer-domain">
      <name>Defer custom domain (keep Railway URL)</name>
      <pros>No action needed. Wiki is already live and functional.</pros>
      <cons>SC1 marked as deferred. URL is long and unmemorable.</cons>
    </option>
  </options>
  <resume-signal>Select: configure-domain (and provide domain name), or defer-domain</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Final verification of all Phase 9 success criteria</name>
  <files></files>
  <action>
Run a comprehensive verification of all Phase 9 success criteria against the live wiki:

1. **SC1 - Custom domain:** If user chose configure-domain, verify DNS resolves and HTTPS cert is valid via `curl -sI https://<domain>`. If deferred, note as deferred.

2. **SC2 - Backups:** Verify `.github/workflows/wiki-backup.yml` exists. Run `cd wiki && python -m sync.backup --output-dir ./backup_verify` and confirm XML output has 50+ pages. Clean up.

3. **SC3 - Deck building guide:** Verify the page exists on the live wiki:
   ```
   cd wiki && python -c "
   from sync.client import get_site
   site = get_site()
   page = site.pages['Deck Building Guide']
   assert page.exists, 'Deck Building Guide page missing'
   text = page.text()
   assert 'AUTO-ARCHETYPES' in text, 'Auto-archetypes section missing'
   print(f'OK: Deck Building Guide exists ({len(text)} chars)')
   "
   ```

4. **SC4 - Mobile responsive:** Verify MediaWiki:Common.css contains the mobile responsive rules:
   ```
   cd wiki && python -c "
   from sync.client import get_site
   site = get_site()
   css = site.pages['MediaWiki:Common.css'].text()
   assert 'Grid Tactics Mobile' in css, 'Mobile CSS block missing'
   print('OK: Mobile CSS present')
   "
   ```

5. **SC5 - Logo and favicon:** Verify uploads exist:
   ```
   cd wiki && python -c "
   from sync.client import get_site
   site = get_site()
   logo = site.images['Wiki.png']
   print(f'Logo: {\"exists\" if logo.exists else \"MISSING\"}'  )
   "
   ```

6. **SC6 - Search:** Run `cd wiki && python -m sync.sync_wiki --verify-search` and confirm exit 0.

Print a final report table:
```
Phase 9 Verification Report
SC1 Custom domain:    [PASS/DEFERRED]
SC2 Backups:          [PASS/FAIL]
SC3 Deck guide:       [PASS/FAIL]
SC4 Mobile CSS:       [PASS/FAIL]
SC5 Logo/favicon:     [PASS/FAIL]
SC6 Search:           [PASS/FAIL]
```
  </action>
  <verify>All 5 non-deferred success criteria show PASS.</verify>
  <done>Phase 9 verification report shows all success criteria met (custom domain PASS or DEFERRED based on user decision).</done>
</task>

</tasks>

<verification>
Final verification report covers all 6 success criteria from the Phase 9 roadmap entry.
</verification>

<success_criteria>
- Custom domain configured OR explicitly deferred by user
- All other success criteria verified PASS against live wiki
- Phase 9 complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-launch-polish/09-04-SUMMARY.md`
</output>
