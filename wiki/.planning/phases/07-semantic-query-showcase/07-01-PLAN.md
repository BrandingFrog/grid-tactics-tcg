---
phase: 07-semantic-query-showcase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wiki/sync/sync_homepage.py
  - wiki/sync/sync_wiki.py
autonomous: true

must_haves:
  truths:
    - "Main Page links to element, tribe, keyword, rules, and patch indexes"
    - "All links on Main Page resolve to existing wiki pages"
    - "Main Page is live on the Railway wiki"
  artifacts:
    - path: "wiki/sync/sync_homepage.py"
      provides: "Main Page wikitext generation and upsert"
      exports: ["main_page_wikitext", "sync_main_page"]
  key_links:
    - from: "wiki/sync/sync_wiki.py"
      to: "wiki/sync/sync_homepage.py"
      via: "--homepage CLI flag"
      pattern: "sync_homepage"
---

<objective>
Create the wiki Main Page with navigation links to all major index pages (elements, tribes, keywords, rules, patches, all cards).

Purpose: The Main Page is the entry point for the wiki. Without it, users must know page names to navigate. This satisfies requirement WIKI-08.
Output: `sync_homepage.py` module + `--homepage` CLI flag on sync_wiki.py + Main Page live on Railway wiki.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@wiki/.planning/PROJECT.md
@wiki/.planning/ROADMAP.md
@wiki/.planning/STATE.md
@wiki/sync/sync_wiki.py
@wiki/sync/sync_taxonomy.py
@wiki/sync/client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sync_homepage.py with Main Page wikitext</name>
  <files>wiki/sync/sync_homepage.py</files>
  <action>
Create `wiki/sync/sync_homepage.py` with two functions:

1. `main_page_wikitext() -> str`: Returns the wikitext for the Main Page. Content should include:
   - A welcome header: "Welcome to the Grid Tactics Wiki"
   - Brief intro paragraph mentioning it's the knowledge base for Grid Tactics TCG
   - Section "== Card Database ==" with links:
     - `[[:Category:Card|All Cards]]` (browse all cards)
     - `[[Semantic:Showcase|Semantic Queries]]` (link to Phase 7 plan 02's page)
   - Section "== Elements ==" with inline links to all 7 elements: Wood, Fire, Earth, Water, Metal, Dark, Light. Also link `[[:Category:Element|All Elements]]`.
   - Section "== Tribes ==" with link to `[[:Category:Tribe|All Tribes]]` (too many to list inline).
   - Section "== Keywords ==" with links to `[[:Category:Keyword|All Keywords]]`, `[[:Category:Trigger Keyword|Trigger Keywords]]`, `[[:Category:Mechanic Keyword|Mechanic Keywords]]`.
   - Section "== Rules ==" with links to: [[Grid Tactics TCG]], [[5x5 Board]], [[Mana]], [[React Window]], [[Win Conditions]], [[Turn Structure]].
   - Section "== Patch Notes ==" with link to [[Patch:Index]].
   - Footer: `[[Category:Rules]]` to keep Main_Page in the rules category.

2. `sync_main_page(site, dry_run=False) -> str`: Upserts the Main Page to the wiki. Use `site.pages["Main Page"]` (MediaWiki's canonical main page title). Return status string ("created", "updated", "unchanged"). Follow the same `rstrip()` idempotency pattern from `sync_taxonomy.py`.

Do NOT use the `upsert_taxonomy_pages()` helper -- Main Page is a single special page, direct upsert is cleaner.
  </action>
  <verify>
    `cd wiki && python -c "from sync.sync_homepage import main_page_wikitext; t = main_page_wikitext(); assert '[[Fire]]' in t; assert '[[Patch:Index]]' in t; assert 'Category:Card' in t; assert 'Semantic:Showcase' in t; print('OK')"` passes.
  </verify>
  <done>
    `main_page_wikitext()` returns wikitext containing links to all 7 elements, all 6 rules pages, Category:Card, Category:Tribe, Category:Keyword, Category:Element, Patch:Index, and Semantic:Showcase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire --homepage flag into sync_wiki.py CLI and push to live wiki</name>
  <files>wiki/sync/sync_wiki.py</files>
  <action>
1. Add `--homepage` to the mutually exclusive group in `sync_wiki.py`'s CLI parser. When invoked, it calls `sync_main_page(site, dry_run=args.dry_run)` from `sync_homepage`.

2. Run `cd wiki && python -m sync.sync_wiki --homepage` to push Main Page to the live Railway wiki.

3. Verify by reading the page back: `site.pages["Main Page"].text()` should contain the expected links.
  </action>
  <verify>
    `cd wiki && python -m sync.sync_wiki --homepage --dry-run` runs without error and reports "unchanged" (since we just pushed it).
  </verify>
  <done>
    Main Page is live on Railway wiki with links to elements, tribes, keywords, rules, patches, and card database. `--homepage` CLI flag works for future re-syncs.
  </done>
</task>

</tasks>

<verification>
- `cd wiki && python -m sync.sync_wiki --homepage --dry-run` reports "unchanged"
- Main Page on Railway wiki contains links to all index categories
</verification>

<success_criteria>
Main Page is live on the Railway wiki with working navigation links to element, tribe, keyword, rules, and patch indexes. All linked pages exist (created in Phases 3-6).
</success_criteria>

<output>
After completion, create `.planning/phases/07-semantic-query-showcase/07-01-SUMMARY.md`
</output>
