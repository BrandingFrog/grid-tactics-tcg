# End-to-end tests

This folder contains browser-driven checks that validate complete user journeys against a running server. E2E tests are slower and more timing-sensitive than unit tests, so reserve them for high-value flows such as joining, pregame choices, taking a turn, reconnecting, and spectating.

Keep screenshots and traces out of the repository unless they are deliberate evidence under `.planning/debug/`.
