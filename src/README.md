# Source package

`src/grid_tactics/` is the installable Python package. Keeping package code under `src/` prevents tests and scripts from accidentally importing files directly from the repository root.

See `grid_tactics/README.md` for the subsystem map.
