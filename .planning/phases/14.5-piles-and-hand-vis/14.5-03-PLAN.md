---
phase: 14.5-piles-and-hand-vis
plan: 03
type: execute
wave: 3
depends_on: ["14.5-01"]
files_modified:
  - src/grid_tactics/server/view_filter.py
autonomous: true
must_haves:
  truths:
    - "Both players' graveyards are serialized as public card_numeric_id lists to every viewer"
    - "Both players' exhaust piles are serialized as public card_numeric_id lists to every viewer"
    - "Opponent hand is still serialized as COUNT ONLY (no card identities) — hidden info preserved"
    - "Opponent hand count is included so the opp-hand-row can render N face-down backs"
  artifacts:
    - path: "src/grid_tactics/server/view_filter.py"
      provides: "piles serialization for both players"
  key_links:
    - from: "view_filter output frame"
      to: "players[*].graveyard + players[*].exhaust"
      via: "full id list, public info"
    - from: "view_filter output frame"
      to: "players[opp].hand_count"
      via: "int count, no ids"
---

<objective>
Serialize graveyard and exhaust piles as public info to both players and spectators. Preserve opponent-hand hidden-info security (count only).

Purpose: Frontend cannot render pile modals or opp-hand-row without this payload.
Output: view_filter emits graveyard, exhaust (full ids both sides) and opponent hand_count.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14.5-piles-and-hand-vis/14.5-01-SUMMARY.md
@src/grid_tactics/server/view_filter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialize both piles for both players</name>
  <files>src/grid_tactics/server/view_filter.py</files>
  <action>
  In the per-player view building function:
  - For the viewing player: emit `players[me] = {hand: [...full cards...], graveyard: [...ids...], exhaust: [...ids...], ...}`
  - For the opponent: emit `players[opp] = {hand_count: N, graveyard: [...ids...], exhaust: [...ids...], ...}` — NO hand card ids.
  - Spectator view: both players get hand_count only, full graveyard + exhaust for both.

  Graveyard and exhaust are tuples of card_numeric_ids → serialize as JSON arrays of ints.

  Do NOT leak opponent hand card identities under any code path. Add an assertion in a test if possible.
  </action>
  <verify>pytest tests/ -q -k "view_filter or view"</verify>
  <done>View frames include graveyard + exhaust for both players; opponent hand is count-only. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Test piles serialization + hand hidden-info</name>
  <files>tests/test_view_filter.py</files>
  <action>
  Add tests (create the file if it doesn't exist; otherwise extend):
  1. `test_view_filter_emits_both_graveyards` — setup state with ids in both graveyards, build view from player 0 perspective, assert both graveyards present.
  2. `test_view_filter_emits_both_exhausts` — same for exhaust.
  3. `test_view_filter_hides_opponent_hand_ids` — put cards in opp hand, build view, assert opponent hand is count-only (no 'hand' key with ids, or empty list; just hand_count).
  </action>
  <verify>pytest tests/test_view_filter.py -q</verify>
  <done>All 3 tests pass. Hidden-info property holds.</done>
</task>

</tasks>

<verification>
pytest tests/ -q fully green.
</verification>

<success_criteria>
view_filter emits public graveyard + exhaust for both players, opponent hand stays hidden as count only.
</success_criteria>

<output>
Create `.planning/phases/14.5-piles-and-hand-vis/14.5-03-SUMMARY.md`
</output>
