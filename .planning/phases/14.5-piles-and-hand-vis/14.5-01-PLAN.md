---
phase: 14.5-piles-and-hand-vis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/grid_tactics/minion.py
  - src/grid_tactics/player.py
  - src/grid_tactics/game_state.py
  - src/grid_tactics/action_resolver.py
  - src/grid_tactics/effect_resolver.py
  - tests/test_action_resolver.py
autonomous: true
must_haves:
  truths:
    - "Minions deployed from hand have from_deck=True"
    - "Minions spawned via conjure/token paths have from_deck=False"
    - "When a from_deck=True minion dies, its card_numeric_id is appended to owner's graveyard"
    - "When a from_deck=False minion dies, nothing is added to graveyard (token vanishes)"
    - "Magic/react cards played as one-shots append to owner's graveyard"
    - "Cards discarded for cost (summon_sacrifice_tribe) append to owner's exhaust"
  artifacts:
    - path: "src/grid_tactics/minion.py"
      provides: "MinionInstance.from_deck: bool = True field"
    - path: "src/grid_tactics/player.py"
      provides: "Player.exhaust: tuple[int, ...] = () field"
    - path: "src/grid_tactics/action_resolver.py"
      provides: "Death cleanup respects from_deck; one-shot play routes to graveyard; discard-for-cost routes to exhaust"
    - path: "tests/test_action_resolver.py"
      provides: "Graveyard/exhaust/token-exclusion tests"
  key_links:
    - from: "action_resolver death handler"
      to: "player.graveyard"
      via: "if minion.from_deck: append card_numeric_id"
    - from: "effect_resolver conjure paths"
      to: "MinionInstance(from_deck=False)"
      via: "explicit kwarg on all token spawns"
---

<objective>
Add graveyard and exhaust pile tracking to the Python engine, with token exclusion via a new MinionInstance.from_deck flag.

Purpose: Foundation for Phase 14.5 pile UI. Without these fields, the frontend cannot display piles.
Output: Engine correctly tracks graveyard (deck-origin cards only) and exhaust (discard-for-cost), with tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/grid_tactics/minion.py
@src/grid_tactics/player.py
@src/grid_tactics/action_resolver.py
@src/grid_tactics/effect_resolver.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add from_deck to MinionInstance and exhaust to Player</name>
  <files>src/grid_tactics/minion.py, src/grid_tactics/player.py, src/grid_tactics/game_state.py</files>
  <action>
  In minion.py: add `from_deck: bool = True` field to MinionInstance dataclass. Update any `replace()` callers that reconstruct a minion to preserve from_deck. Update to_dict/from_dict if present.

  In player.py: add `exhaust: tuple[int, ...] = ()` field to Player dataclass, parallel to `graveyard`. Update to_dict/from_dict mirror.

  In game_state.py: ensure any serialization mirrors both new fields. Default graveyard/exhaust to empty tuple.

  Do NOT change tensor engine in this task (Wave 2 handles parity).
  </action>
  <verify>python -c "from grid_tactics.minion import MinionInstance; from grid_tactics.player import Player; p=Player(); assert p.exhaust == ()"</verify>
  <done>MinionInstance has from_deck (default True), Player has exhaust (default empty tuple), existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire from_deck propagation + graveyard/exhaust population</name>
  <files>src/grid_tactics/action_resolver.py, src/grid_tactics/effect_resolver.py</files>
  <action>
  Propagation rules (document these in code comments):

  Set from_deck=True (default, implicit) on:
  - Normal PLAY_CARD deploy from hand → board

  Set from_deck=False explicitly on ALL of these paths:
  - `_apply_conjure_rat_and_buff` (Ratchanter conjure path)
  - Any `summon_token` / token spawn helper
  - Diodebot tutor-spawn paths IF they create minions directly on the board (if they only add to hand, from_deck=True since the tutored card originated from the deck)
  - Common Rat token spawns (anywhere they appear)
  - Any future effect that creates a minion not drawn from the deck

  Death cleanup (in action_resolver death handler / remove-from-board path):
  - When a minion is removed due to HP<=0, check `if minion.from_deck:` — if True, append `minion.card_numeric_id` to `state.players[minion.owner].graveyard`. If False, append nothing (token vanishes).

  One-shot play (magic/react cards):
  - After resolving a magic or react card from hand, append its card_numeric_id to the casting player's graveyard. These always count as "from deck".

  Discard-for-cost (exhaust):
  - Locate `summon_sacrifice_tribe` mechanic (and any other path that removes a card from hand or deck as a cost without playing it). Append the discarded card's numeric_id to owner's exhaust tuple.

  Use `dataclasses.replace` + tuple concatenation to keep immutability.
  </action>
  <verify>pytest tests/ -q -x</verify>
  <done>All existing tests pass. Conjure paths construct minions with from_deck=False. Death handler gates graveyard append on from_deck. One-shots and discard-for-cost correctly populate graveyard/exhaust.</done>
</task>

<task type="auto">
  <name>Task 3: Tests for graveyard token exclusion and exhaust</name>
  <files>tests/test_action_resolver.py</files>
  <action>
  Add tests:
  1. `test_minion_death_adds_to_graveyard` — deploy a minion from hand, kill it, assert card_numeric_id is in owner.graveyard.
  2. `test_token_death_does_not_add_to_graveyard` — trigger Ratchanter conjure (or other token spawn), kill the token, assert graveyard is unchanged.
  3. `test_magic_play_goes_to_graveyard` — play any magic card, assert its id is in graveyard.
  4. `test_discard_for_cost_goes_to_exhaust` — trigger summon_sacrifice_tribe or equivalent discard-for-cost path, assert discarded card id is in owner.exhaust and NOT in graveyard.
  5. `test_conjured_minion_has_from_deck_false` — spawn a token, read the MinionInstance, assert from_deck is False.
  </action>
  <verify>pytest tests/test_action_resolver.py -q</verify>
  <done>All 5 new tests pass. Full test suite still green.</done>
</task>

</tasks>

<verification>
pytest tests/ -q must be fully green. grep for `from_deck=False` in action_resolver.py and effect_resolver.py and confirm every conjure/token path is covered.
</verification>

<success_criteria>
Engine correctly tracks graveyard (from-deck cards only) and exhaust (discard-for-cost), with token exclusion verified by tests.
</success_criteria>

<output>
Create `.planning/phases/14.5-piles-and-hand-vis/14.5-01-SUMMARY.md`
</output>
