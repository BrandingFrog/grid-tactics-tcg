---
phase: 14.5-piles-and-hand-vis
plan: 05
type: execute
wave: 5
depends_on: ["14.5-03", "14.5-04"]
files_modified:
  - src/grid_tactics/server/static/game.html
  - src/grid_tactics/server/static/game.js
  - src/grid_tactics/server/static/game.css
autonomous: true
must_haves:
  truths:
    - "Four emoji buttons visible: own graveyard (bottom-right), own exhaust (bottom-right next to graveyard), opp graveyard (top-right), opp exhaust (top-right next to opp graveyard)"
    - "Each button shows a count with the black stroke-halo drop-shadow like other stat emojis"
    - "Clicking any button opens a modal via showPileModal(title, cardNumericIds)"
    - "Modal renders all cards in the pile via renderCardFrame(card, {context:'pile'}) in a scrollable grid"
    - "Opponent hand row at top of screen shows N face-down card backs, updated on every state frame"
    - "Opponent hand row contains no card identity data (only count)"
  artifacts:
    - path: "src/grid_tactics/server/static/game.html"
      provides: "4 pile buttons + opp-hand-row container divs"
    - path: "src/grid_tactics/server/static/game.js"
      provides: "showPileModal(title, ids) function + renderOppHandRow(count) function + pile button bindings"
    - path: "src/grid_tactics/server/static/game.css"
      provides: "pile button styles, pile modal styles, opp-hand-row styles, face-down card back style"
  key_links:
    - from: "pile button click handlers"
      to: "showPileModal"
      via: "getCardNumericIds from current state frame's graveyard/exhaust arrays"
    - from: "applyStateFrame render pass"
      to: "renderOppHandRow(opp.hand_count)"
      via: "called every frame"
    - from: "showPileModal grid cells"
      to: "renderCardFrame (Wave 4)"
      via: "context: 'pile'"
---

<objective>
Add the four pile UI buttons, the shared pile modal, and the opponent face-down hand row.

Purpose: Make graveyard, exhaust, and opp hand visible to players.
Output: 4 buttons + modal + opp hand row wired to live state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14.5-piles-and-hand-vis/14.5-03-SUMMARY.md
@.planning/phases/14.5-piles-and-hand-vis/14.5-04-SUMMARY.md
@src/grid_tactics/server/static/game.html
@src/grid_tactics/server/static/game.js
@src/grid_tactics/server/static/game.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML + CSS for pile buttons and opp-hand-row</name>
  <files>src/grid_tactics/server/static/game.html, src/grid_tactics/server/static/game.css</files>
  <action>
  In game.html, add:
  - `<div class="opp-hand-row" id="oppHandRow"></div>` placed above the board container, below the opponent info bar.
  - Four buttons with class `.pile-btn` and distinct ids:
    - `#pileBtnOwnGraveyard` (bottom-right of own play area)
    - `#pileBtnOwnExhaust` (bottom-right, next to own graveyard)
    - `#pileBtnOppGraveyard` (top-right of opp area)
    - `#pileBtnOppExhaust` (top-right, next to opp graveyard)
  - A modal container `#pileModal` with backdrop, title, scrollable grid area, close button (reuse existing modal pattern from tutor modal / deck-code modal).

  Each button structure:
  ```html
  <button class="pile-btn" id="pileBtnOwnGraveyard">
    <span class="pile-emoji">⚰️</span>
    <span class="pile-count stat-emoji-halo">0</span>
  </button>
  ```
  Use `⚰️` for graveyard and `🌀` for exhaust.

  In game.css:
  - `.pile-btn` — absolutely positioned in the play-area corners, button reset, cursor pointer, small gap between stacked buttons.
  - `.pile-emoji` — font-size ~28px.
  - `.pile-count` — reuse existing `.stat-emoji-halo` / text-stroke drop-shadow hack used by 🗡️/🤍 stats.
  - `.opp-hand-row` — flex row, gap 4px, centered, small card-back tiles (~60x84 px).
  - `.opp-hand-card-back` — fixed size, border, gradient or pattern background (use existing card back asset if present, else CSS gradient), no hover interaction.
  - `.pile-modal` — overlay, centered panel, scrollable `.pile-grid` using CSS grid repeat(auto-fill, minmax(180px, 1fr)) or whatever the deck builder uses.
  </action>
  <verify>Load page, visually confirm buttons appear in correct corners and opp hand row renders above board.</verify>
  <done>DOM and styles in place. Buttons and row visible but not yet wired to data.</done>
</task>

<task type="auto">
  <name>Task 2: showPileModal + pile button wiring + renderOppHandRow</name>
  <files>src/grid_tactics/server/static/game.js</files>
  <action>
  Add three functions:

  1. `function showPileModal(title, cardNumericIds) { ... }` — shared for all 4 buttons. Sets modal title, clears the grid, maps ids → card data via existing CardLibrary lookup, calls `renderCardFrame(card, {context:'pile', interactive:false})` for each and appends to grid. Shows the modal. Close button and backdrop click dismiss.

  2. `function renderOppHandRow(count) { ... }` — clears `#oppHandRow`, appends `count` `<div class="opp-hand-card-back"></div>` elements. Called from `applyStateFrame` or `renderGame` every frame.

  3. `function updatePileButtonCounts(state) { ... }` — reads state.players[me].graveyard.length etc., updates the `.pile-count` text in each button. Called from renderGame.

  Wire button click handlers (once, on init):
  ```js
  document.getElementById('pileBtnOwnGraveyard').addEventListener('click', () => {
    showPileModal('Your Graveyard', currentState.players[myPlayerIdx].graveyard);
  });
  // ... same for other 3, using opp index for opponent buttons
  ```

  In renderGame (or applyStateFrame's render pass), call `renderOppHandRow(oppHandCount)` and `updatePileButtonCounts(state)`.
  </action>
  <verify>Start server, play a game against self in two tabs. Kill a minion → own graveyard count ticks to 1, opp graveyard count ticks to 1 on the other tab. Click the button → modal shows the card. Draw cards → opp hand row updates face-down count.</verify>
  <done>All 4 buttons show live counts, all 4 modals render the correct piles via uniform renderer, opp hand row reflects live count.</done>
</task>

</tasks>

<verification>
Two-tab manual test: deploy minion, kill it, verify graveyard updates on both sides and modal shows the card. Discard-for-cost (summon_sacrifice_tribe minion) → exhaust updates. Opp hand count changes when they draw.
</verification>

<success_criteria>
4 pile buttons live, shared showPileModal works for all, opp hand row visible and reactive.
</success_criteria>

<output>
Create `.planning/phases/14.5-piles-and-hand-vis/14.5-05-SUMMARY.md`
</output>
