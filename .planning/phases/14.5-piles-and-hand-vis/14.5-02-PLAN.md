---
phase: 14.5-piles-and-hand-vis
plan: 02
type: execute
wave: 2
depends_on: ["14.5-01"]
files_modified:
  - src/grid_tactics/tensor_engine/state.py
  - src/grid_tactics/tensor_engine/actions.py
autonomous: true
must_haves:
  truths:
    - "Tensor engine has a per-minion from_deck flag matching Python engine semantics"
    - "Tensor engine tracks per-player exhaust pile counts/contents parallel to graveyard"
    - "Token spawn paths in tensor engine set from_deck=0"
    - "Death cleanup in tensor engine respects from_deck"
    - "Discard-for-cost paths populate exhaust tensor"
  artifacts:
    - path: "src/grid_tactics/tensor_engine/state.py"
      provides: "from_deck and exhaust tensor fields"
    - path: "src/grid_tactics/tensor_engine/actions.py"
      provides: "Death/conjure/discard mirror of Python engine"
  key_links:
    - from: "tensor_engine token spawn"
      to: "minion_from_deck tensor"
      via: "set to 0 on conjure"
---

<objective>
Mirror Phase 14.5 Wave 1 engine changes into the tensor engine so RL training stays consistent with the Python reference.

Purpose: Prevent RL/Python drift. Flag cumulative checkpoint staleness.
Output: Tensor engine has from_deck + exhaust tracking. Existing tensor tests pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14.5-piles-and-hand-vis/14.5-01-SUMMARY.md
@src/grid_tactics/tensor_engine/state.py
@src/grid_tactics/tensor_engine/actions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add from_deck + exhaust tensors</name>
  <files>src/grid_tactics/tensor_engine/state.py</files>
  <action>
  Add two new tensor fields to the batched state:
  - `minion_from_deck: torch.Tensor` shape [N, max_minions] dtype bool (default True for deployed-from-hand).
  - `exhaust: torch.Tensor` shape [N, 2, max_pile_size] int (parallel to existing graveyard tensor; reuse its capacity and pointer pattern). Also an `exhaust_count` tensor [N, 2] if that pattern is used for graveyard.

  Initialize both to empty/default in reset_batch. Zero out on reset.
  </action>
  <verify>python -c "from grid_tactics.tensor_engine.state import TensorState; import torch; s=TensorState.create(4); assert s.exhaust.shape[0]==4"</verify>
  <done>Tensors exist, reset works, shapes consistent with graveyard.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror from_deck propagation and graveyard/exhaust writes</name>
  <files>src/grid_tactics/tensor_engine/actions.py</files>
  <action>
  Mirror Python engine rules from Wave 1:
  - Deploy-from-hand: set minion_from_deck[b, slot] = True.
  - Conjure/token spawn paths: set minion_from_deck[b, slot] = False.
  - Death cleanup: use a mask `from_deck_mask & died_mask` when appending to graveyard. Do NOT append for tokens.
  - One-shot magic/react: append to graveyard (unchanged if already done, else add).
  - Discard-for-cost: append to exhaust tensor.

  Do NOT modify action space (still 1262). Do NOT change reward shaping. Flag RL checkpoint staleness in SUMMARY.
  </action>
  <verify>pytest tests/ -q -k "tensor" </verify>
  <done>Tensor tests pass. from_deck and exhaust tensors correctly populated across all paths.</done>
</task>

</tasks>

<verification>
pytest tests/ -q fully green. Manual diff: for a scripted batch of 4 games, Python engine and tensor engine produce identical graveyard/exhaust contents after same action sequence.
</verification>

<success_criteria>
Tensor engine parity for from_deck and exhaust. RL action space unchanged. Checkpoint staleness flagged in SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/14.5-piles-and-hand-vis/14.5-02-SUMMARY.md` and note RL checkpoint staleness.
</output>
