---
phase: 14.5-piles-and-hand-vis
plan: 06
type: execute
wave: 6
depends_on: ["14.5-05"]
files_modified:
  - src/grid_tactics/server/static/game.js
  - src/grid_tactics/server/static/game.css
autonomous: true
must_haves:
  truths:
    - "When own hand gains a card (any source: draw, tutor, conjure, react-gain) an animation plays: card flies from source → scales to hand-card size → settles into hand row"
    - "When opponent hand gains a card, their opp-hand-row gets a face-down card popping/sliding in at the end"
    - "Animations are enqueued via the Phase 14.3 AnimationQueue (enqueueAnimation) with new job types 'draw_own' and 'draw_opp'"
    - "Multiple simultaneous draws play IN ORDER, not skipped, respecting queue semantics"
    - "Own-hand draw animation is ~600ms; opp-hand draw animation is ~400ms"
  artifacts:
    - path: "src/grid_tactics/server/static/game.js"
      provides: "playDrawOwnAnimation(job,done), playDrawOppAnimation(job,done), hand-diff detection in applyStateFrame"
    - path: "src/grid_tactics/server/static/game.css"
      provides: "@keyframes for draw-fly-in and opp-back-pop"
  key_links:
    - from: "applyStateFrame state diff"
      to: "enqueueAnimation({type:'draw_own'|'draw_opp', ...})"
      via: "prev.hand vs next.hand size + identity diff"
    - from: "runQueue switch statement"
      to: "playDrawOwnAnimation / playDrawOppAnimation"
      via: "new case branches (matching existing 'summon'|'attack'|'move' pattern at game.js:1820)"
---

<objective>
Integrate card-to-hand animations with the Phase 14.3 AnimationQueue for both own and opponent hands.

Purpose: Must-have #7. Players see draws happen instead of cards silently appearing.
Output: Two new animation job types wired into the queue, triggered by hand-size diffs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14.3-game-juice/14.3-01-SUMMARY.md
@.planning/phases/14.5-piles-and-hand-vis/14.5-05-SUMMARY.md
@src/grid_tactics/server/static/game.js
@src/grid_tactics/server/static/game.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add draw_own + draw_opp job types to AnimationQueue</name>
  <files>src/grid_tactics/server/static/game.js, src/grid_tactics/server/static/game.css</files>
  <action>
  In game.js around the existing switch at line ~1820 (`switch (job && job.type)` inside runQueue, next to 'summon'|'attack'|'move'), add two new cases:
  ```js
  case 'draw_own':
      playDrawOwnAnimation(job, done);
      return;
  case 'draw_opp':
      playDrawOppAnimation(job, done);
      return;
  ```

  Implement:
  - `playDrawOwnAnimation(job, done)`: job carries `{type, cardNumericId, fromPos: {x,y}|'deck'|'center', toSlotIndex}`. Create a floating card element at fromPos using `renderCardFrame(card, {context:'hand', interactive:false})`, position absolute, start small/transparent, animate via CSS class `.draw-fly-in` for ~600ms to the target hand slot's screen coords. On animationend, remove the floater, call `applyStateFrame` (by setting `job.stateApplied = false` so runQueue does it, OR apply inline and set stateApplied=true — match the convention used by other branches), then call `done()`. Total ~600ms.

  - `playDrawOppAnimation(job, done)`: add a new `.opp-hand-card-back` element to `#oppHandRow` with a `.pop-in` keyframe class. ~400ms. Call done() on animationend. stateApplied handling: let applyStateFrame re-render the row naturally, which will idempotently replace it.

  In game.css add keyframes:
  - `@keyframes draw-fly-in` — from scale(0.3) translate(fromX,fromY) opacity 0 → to scale(1) translate(0,0) opacity 1, ease-out, 600ms.
  - `@keyframes opp-back-pop` — scale 0 → 1.1 → 1, 400ms ease-out.
  - `.draw-fly-in { animation: draw-fly-in 600ms ease-out forwards; position:absolute; z-index:1000; pointer-events:none; }`
  - `.opp-hand-card-back.pop-in { animation: opp-back-pop 400ms ease-out; }`
  </action>
  <verify>Manually enqueue a test job from devtools: `enqueueAnimation({type:'draw_own', cardNumericId: 1, fromPos:'center', toSlotIndex:0})` — animation plays.</verify>
  <done>Both job types integrated into runQueue switch. CSS keyframes work in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Diff hand in applyStateFrame to enqueue draw animations</name>
  <files>src/grid_tactics/server/static/game.js</files>
  <action>
  In the state-frame handler (where applyStateFrame is called from the websocket message path, NOT inside applyStateFrame itself — animations must be enqueued BEFORE the queue decides to apply the new frame):

  Compare `prev.players[me].hand` vs `next.players[me].hand`:
  - For each newly-added card id (cards in next but not in prev, accounting for duplicates), enqueue `{type:'draw_own', cardNumericId: id, fromPos: 'center' (or 'deck' if we have a deck pile position), toSlotIndex: idx}`.

  Compare `prev.players[opp].hand_count` vs `next.players[opp].hand_count`:
  - If increased by K, enqueue K `{type:'draw_opp'}` jobs.

  Multiple simultaneous draws (tutor chains): enqueue all in order. The queue already serializes.

  Ensure this hooks into the SAME dispatcher as other animation-producing frames (look for the existing prev/next diff logic around game.js:2467-2495 that creates summon/move/attack jobs and co-locate the draw diff there).
  </action>
  <verify>Two-tab self play: draw a card → fly-in animation plays on your side, face-down pop plays on opp side. Play Diodebot or Ratchanter tutor → correct number of draw animations play in order.</verify>
  <done>Hand-size changes reliably trigger the right animations in both tabs.</done>
</task>

</tasks>

<verification>
Multi-tab smoke test with tutor effects (Diodebot, Ratchanter) to confirm chains animate in order. No frames skipped. No z-index issues with modal/hand-row.
</verification>

<success_criteria>
Draw animations play via AnimationQueue for both own and opponent hands, integrated with Phase 14.3 queue semantics.
</success_criteria>

<output>
Create `.planning/phases/14.5-piles-and-hand-vis/14.5-06-SUMMARY.md`
</output>
