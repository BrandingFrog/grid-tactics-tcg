---
phase: 14.5-piles-and-hand-vis
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - src/grid_tactics/server/static/game.js
  - src/grid_tactics/server/static/game.css
autonomous: true
must_haves:
  truths:
    - "A single shared card-frame renderer produces the DOM used by deck builder, hand, tooltip preview, and pile modals"
    - "Hand cards and deck builder cards render visually identically (same art, frame, stats, keywords layout)"
    - "No duplicated card-rendering code remains between renderHandCard and renderDeckBuilderCard"
  artifacts:
    - path: "src/grid_tactics/server/static/game.js"
      provides: "renderCardFrame(card, opts) shared base OR renderHandCard collapsed into renderDeckBuilderCard"
  key_links:
    - from: "renderHandCard callsite"
      to: "shared renderCardFrame"
      via: "opts.context='hand' or similar mode"
    - from: "pile modal (Wave 5)"
      to: "shared renderCardFrame"
      via: "opts.context='pile'"
---

<objective>
Refactor card rendering so hand, deck builder, tooltip preview, and (future) pile modals all use one shared full-card component. Zero visual drift.

Purpose: Must-have #5. Prerequisite for Wave 5 pile modals looking consistent.
Output: Single renderer function; renderHandCard either deleted or becomes a thin wrapper.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/grid_tactics/server/static/game.js
@src/grid_tactics/server/static/game.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared renderCardFrame</name>
  <files>src/grid_tactics/server/static/game.js, src/grid_tactics/server/static/game.css</files>
  <action>
  Audit `renderDeckBuilderCard` and `renderHandCard` in game.js. Identify the superset of DOM nodes (art, name, cost, atk/hp, keywords, type badge, rarity, etc.).

  Create `renderCardFrame(card, opts)` where opts = { context: 'deck-builder' | 'hand' | 'tooltip' | 'pile', interactive: bool, showCost: bool, onClick?: fn }. It returns a DOM element with class `.card-frame-full` plus an optional context class (`.card-frame-full--hand`, `.card-frame-full--pile`).

  Collapse `renderHandCard` into a wrapper that calls `renderCardFrame(card, {context:'hand', interactive:true, onClick: playCard})`. Callers of renderHandCard stay unchanged.

  Update `renderDeckBuilderCard` to call `renderCardFrame(card, {context:'deck-builder', ...})`.

  Update any tooltip preview path to use `renderCardFrame(card, {context:'tooltip'})`.

  In game.css: consolidate `.card-frame-full` and `.card-frame-hand` styles. If they diverged, merge to a single base `.card-frame-full` with tiny context modifiers for hand (e.g., scale, hover lift). Delete the now-dead `.card-frame-hand` rules. Hand layout positioning (row flex) stays on the parent container, not the card itself.
  </action>
  <verify>Manual: load game in browser (python -m grid_tactics.server), open deck builder and in-game hand, visually compare one card shared between both (e.g., Common Rat). Must be pixel-identical modulo hover/scale.</verify>
  <done>One renderer. renderHandCard is either deleted or a 3-line wrapper. Deck builder + hand render identically. No regressions in deck builder interactions or hand playability.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke existing hand interactions</name>
  <files>src/grid_tactics/server/static/game.js</files>
  <action>
  Manually click a card in hand to play it — onClick still fires. Hover tooltip preview still works. Drag-reorder if it exists still works. Deck builder add/remove still works. Fix any regressions from the refactor.
  </action>
  <verify>Start server, open browser, play a card from hand, open deck builder, modify deck. No console errors.</verify>
  <done>No regressions.</done>
</task>

</tasks>

<verification>
Visual parity between deck builder and hand. Browser console clean. All existing hand/deck-builder interactions work.
</verification>

<success_criteria>
Single card renderer shared across all full-size card display contexts.
</success_criteria>

<output>
Create `.planning/phases/14.5-piles-and-hand-vis/14.5-04-SUMMARY.md` documenting the new renderCardFrame API.
</output>
