---
phase: 13-board-hand-ui
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - src/grid_tactics/server/static/game.js
  - src/grid_tactics/server/static/game.html
  - src/grid_tactics/server/static/game.css
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04]
must_haves:
  truths:
    - "Two browser windows show the game board with correct perspectives after create+join+ready"
    - "Board minions display name, ATK/HP, owner color, and attribute circle"
    - "Hand cards show YGO-style rendering with type colors, mana badge, attribute, and dimming"
    - "Both players' HP and Mana are visible"
    - "Turn and phase indicators update correctly between players"
    - "Deck builder renders all cards and saves decks to localStorage"
  artifacts:
    - path: "src/grid_tactics/server/static/game.js"
      provides: "Polished, integration-tested client JS"
    - path: "src/grid_tactics/server/static/game.html"
      provides: "Final HTML with any adjustments from integration testing"
    - path: "src/grid_tactics/server/static/game.css"
      provides: "Final CSS with any visual fixes"
  key_links:
    - from: "src/grid_tactics/server/static/game.js"
      to: "src/grid_tactics/server/events.py"
      via: "Socket.IO bidirectional events for full game flow"
      pattern: "socket\\.emit.*socket\\.on"
---

<objective>
Integration testing, bug fixes, and visual verification of the complete game UI.

Purpose: Ensure all three screens (lobby, deck builder, game board) work end-to-end with two browser windows connected to the same server. Fix any integration issues discovered during testing. Visual checkpoint confirms the UI meets all requirements before the phase is considered complete.

Output: Polished game UI that passes visual inspection for all 4 requirements (UI-01 through UI-04)
</objective>

<execution_context>
@C:\Users\corma\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\corma\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-board-hand-ui/13-CONTEXT.md
@.planning/phases/13-board-hand-ui/13-UI-SPEC.md
@.planning/phases/13-board-hand-ui/13-01-SUMMARY.md
@.planning/phases/13-board-hand-ui/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration smoke test and bug fixes</name>
  <files>src/grid_tactics/server/static/game.js, src/grid_tactics/server/static/game.html, src/grid_tactics/server/static/game.css</files>
  <read_first>
    src/grid_tactics/server/static/game.js
    src/grid_tactics/server/static/game.html
    src/grid_tactics/server/static/game.css
    src/grid_tactics/server/events.py
    src/grid_tactics/server/app.py
  </read_first>
  <action>
Run the full integration flow programmatically and fix any issues found.

**Step 1: Verify Flask serves all assets correctly**
```python
from grid_tactics.server.app import create_app
app = create_app(testing=True)
client = app.test_client()

# Test each static file serves correctly
assert client.get('/').status_code == 200  # game.html
assert client.get('/static/game.css').status_code == 200
assert client.get('/static/game.js').status_code == 200
```

**Step 2: Verify Socket.IO event flow using SocketIOTestClient**
Run through the full create -> join -> ready -> game_start flow and verify:
- game_start event includes enhanced card_defs with card_id, effects, tribe
- state includes minions array, players array with hand/hp/mana
- Verify card_defs keys match what game.js expects (numeric IDs as keys)

**Step 3: Verify JS has no syntax errors**
Use a basic check: ensure the JS file is valid by checking for balanced braces, no obvious issues.

**Step 4: Fix any discovered issues**
Common integration issues to check and fix:
- HTML element IDs matching what JS uses (getElementById targets)
- CSS class names matching what JS generates in innerHTML
- Event payload field names matching between server and client
- Card frame HTML structure matching CSS selectors
- Deck builder form elements matching JS event handlers
- Screen navigation (nav buttons targeting correct screen IDs)
- Lobby form validation (empty name, invalid room code)
- Zone tinting rendering correctly for both player perspectives
- Board minion stats showing current_health (not base health from card_defs)
- Hand card effect text rendering for magic/react cards

**Step 5: Run existing test suite for regression check**
```bash
pytest tests/test_pvp_server.py tests/test_game_flow.py -q --tb=short
```

Fix any regressions.

**Step 6: Final static analysis**
Verify all UI-SPEC requirements are met in the code:
- UI-01: game.js has renderBoard with renderBoardMinion showing name, ATK/HP, owner, attribute
- UI-02: game.js has renderHand with renderHandCard showing name, mana, ATK/HP, effects, attribute + dimming
- UI-03: game.js has renderOpponentInfo + renderSelfInfo showing HP and Mana
- UI-04: game.js has renderRoomBar/Turn showing YOUR TURN/OPPONENT'S TURN + ACTION/REACT badge
  </action>
  <verify>
    <automated>cd "C:/Users/corma/CascadeProjects/card game" && python -c "
from grid_tactics.server.app import create_app
app = create_app(testing=True)
client = app.test_client()
# Verify all three static files serve correctly
r1 = client.get('/')
assert r1.status_code == 200, f'/ returned {r1.status_code}'
assert b'game-board' in r1.data, 'HTML missing game-board'
r2 = client.get('/static/game.css')
assert r2.status_code == 200, f'/static/game.css returned {r2.status_code}'
assert b'card-frame' in r2.data, 'CSS missing card-frame'
r3 = client.get('/static/game.js')
assert r3.status_code == 200, f'/static/game.js returned {r3.status_code}'
assert b'renderBoard' in r3.data, 'JS missing renderBoard'
print('PASS: All static assets serve correctly')
" && python -c "
# Verify enhanced card_defs in game_start flow
from pathlib import Path
from grid_tactics.card_library import CardLibrary
from grid_tactics.server.events import _build_card_defs
lib = CardLibrary.from_directory(Path('data/cards'))
defs = _build_card_defs(lib)
for nid, d in defs.items():
    assert 'card_id' in d, f'Card {nid} missing card_id'
    assert 'name' in d, f'Card {nid} missing name'
    assert 'card_type' in d, f'Card {nid} missing card_type'
    assert 'mana_cost' in d, f'Card {nid} missing mana_cost'
    assert 'element' in d or d.get('element') is None, f'Card {nid} missing element'
print(f'PASS: All {len(defs)} cards have complete card_defs')
" && pytest tests/test_pvp_server.py -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GET / returns 200 with game-board in HTML body
    - GET /static/game.css returns 200 with card-frame in CSS
    - GET /static/game.js returns 200 with renderBoard in JS
    - All card_defs entries have card_id, name, card_type, mana_cost, element fields
    - All existing pvp_server tests pass (no regression)
    - No broken HTML element ID references between game.html and game.js
    - No mismatched CSS class names between game.css and game.js innerHTML
  </acceptance_criteria>
  <done>All static assets serve correctly. Card defs are complete. No integration issues between HTML/CSS/JS. Test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete game UI</name>
  <files>src/grid_tactics/server/static/game.html, src/grid_tactics/server/static/game.css, src/grid_tactics/server/static/game.js</files>
  <action>
Human visual verification checkpoint. Start the PvP server (`python pvp_server.py`) and open two browser windows to `http://localhost:5000`.

Verify Lobby: Create room in Browser 1, join with room code in Browser 2. Both see player list and room code in cyan.

Verify Deck Builder: Click Deck Builder tab. All 21 cards render as YGO-style (gold minions, teal magic, magenta react). Attribute circles show correct colors. Build and save a 30-card deck.

Verify Game Board (UI-01): After both ready up, 5x5 grid appears with zone tinting, minions as compact cards with name/ATK/HP/owner color/attribute. Perspective flip: both players see their zone at bottom.

Verify Hand (UI-02): YGO-style card frames with type backgrounds, mana badge, attribute circle, name, stats. Unaffordable cards dimmed. Hover lifts cards.

Verify Stats (UI-03): HP (green self, red opponent) and Mana (cyan) displayed for both players. Hand/deck counts visible.

Verify Turn Indicator (UI-04): Active player sees "YOUR TURN" green + pulse dot. Other sees "OPPONENT'S TURN" muted. Phase badge shows ACTION (cyan) or REACT (yellow).
  </action>
  <verify>
    <automated>cd "C:/Users/corma/CascadeProjects/card game" && python -c "
from grid_tactics.server.app import create_app
app = create_app(testing=True)
client = app.test_client()
assert client.get('/').status_code == 200
assert client.get('/static/game.css').status_code == 200
assert client.get('/static/game.js').status_code == 200
print('PASS: Server serves all game assets')
"</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms lobby flow works across two browsers
    - Human confirms deck builder renders cards and saves decks
    - Human confirms board shows 5x5 grid with minions, zone tints, and perspective flip
    - Human confirms hand shows YGO-style cards with dimming for unaffordable
    - Human confirms HP/Mana stats visible for both players
    - Human confirms turn/phase indicator updates correctly
  </acceptance_criteria>
  <done>Human approves visual quality of all three screens. Phase 13 requirements UI-01 through UI-04 verified by human inspection.</done>
</task>

</tasks>

<verification>
1. All static assets serve with correct HTTP 200 responses
2. Enhanced card_defs include all required fields
3. Existing test suite passes with no regressions
4. Human visual verification of lobby, deck builder, and game board across two browsers
</verification>

<success_criteria>
- Two browser windows can complete the full flow: lobby -> deck builder -> ready -> game board
- Board shows 5x5 grid with perspective flip, zone tints, and compact minion cards (UI-01)
- Hand shows YGO-style cards with type colors, attributes, mana badge, and dimming (UI-02)
- Both players' HP and Mana displayed prominently (UI-03)
- Turn/phase indicator updates correctly between turns (UI-04)
- Deck builder allows building and saving 30-card decks (D-07, D-08)
- Human approves visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/13-board-hand-ui/13-03-SUMMARY.md`
</output>
