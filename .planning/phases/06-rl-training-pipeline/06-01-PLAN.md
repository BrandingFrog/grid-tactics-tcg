---
phase: 06-rl-training-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/grid_tactics/db/__init__.py
  - src/grid_tactics/db/schema.py
  - src/grid_tactics/db/writer.py
  - src/grid_tactics/db/reader.py
  - tests/test_db.py
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "SB3, sb3-contrib, and PyTorch are installed and importable"
    - "SQLite database schema creates all 5 tables without errors"
    - "Game results can be written and read back from SQLite"
    - "Training run metadata can be stored and queried"
    - "Win rate snapshots can be recorded and retrieved"
    - "Batch insert buffers correctly and flushes on threshold or explicit call"
  artifacts:
    - path: "src/grid_tactics/db/schema.py"
      provides: "SQLite schema creation with WAL mode"
      contains: "CREATE TABLE"
    - path: "src/grid_tactics/db/writer.py"
      provides: "GameResultWriter with buffered batch insert"
      exports: ["GameResultWriter", "TrainingRunWriter"]
    - path: "src/grid_tactics/db/reader.py"
      provides: "Query interface for dashboard reads"
      exports: ["GameResultReader"]
    - path: "tests/test_db.py"
      provides: "Unit tests for all DB operations"
      min_lines: 80
  key_links:
    - from: "src/grid_tactics/db/writer.py"
      to: "src/grid_tactics/db/schema.py"
      via: "ensure_schema() call before writes"
      pattern: "ensure_schema"
    - from: "src/grid_tactics/db/reader.py"
      to: "data/training.db"
      via: "sqlite3.connect"
      pattern: "sqlite3\\.connect"
---

<objective>
Install the RL training stack (SB3 + sb3-contrib + PyTorch) and build the SQLite data persistence layer for game results and training metadata.

Purpose: Nothing in Phase 6 works without SB3 installed. The SQLite layer is an independent foundation that the training pipeline (Plan 02/03) writes to and Phase 9 dashboard reads from.
Output: Importable SB3/sb3-contrib, complete SQLite schema with writer/reader, all DB tests passing.
</objective>

<execution_context>
@C:/Users/corma/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/corma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rl-training-pipeline/06-CONTEXT.md
@.planning/phases/06-rl-training-pipeline/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/grid_tactics/enums.py:
```python
class PlayerSide(IntEnum):
    PLAYER_1 = 0
    PLAYER_2 = 1
```

From src/grid_tactics/game_loop.py:
```python
@dataclass(frozen=True, slots=True)
class GameResult:
    winner: Optional[PlayerSide]
    turn_count: int
    final_hp: tuple[int, int]
    is_draw: bool
    reason: str
```

From src/grid_tactics/card_library.py:
```python
class CardLibrary:
    def get_by_id(self, numeric_id: int) -> CardDefinition: ...
    def get_numeric_id(self, card_id: str) -> int: ...
    def build_deck(self, card_counts: dict[str, int]) -> tuple[int, ...]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install SB3 stack and create SQLite schema + writer</name>
  <read_first>
    - pyproject.toml (current dependencies)
    - .planning/phases/06-rl-training-pipeline/06-RESEARCH.md (SQLite schema pattern, install command)
    - src/grid_tactics/game_loop.py (GameResult dataclass)
  </read_first>
  <files>
    pyproject.toml
    src/grid_tactics/db/__init__.py
    src/grid_tactics/db/schema.py
    src/grid_tactics/db/writer.py
    tests/test_db.py
  </files>
  <behavior>
    - Test: `import sb3_contrib; from sb3_contrib import MaskablePPO` succeeds
    - Test: `import torch` succeeds
    - Test: schema.ensure_schema(db_path) creates 5 tables: training_runs, game_results, deck_compositions, card_actions, win_rate_snapshots
    - Test: schema.ensure_schema() sets PRAGMA journal_mode=WAL
    - Test: GameResultWriter.record_game() buffers results, flush() batch-inserts to SQLite
    - Test: GameResultWriter auto-flushes when buffer reaches buffer_size (default 100)
    - Test: TrainingRunWriter.start_run() creates a row with hyperparameters JSON, started_at timestamp
    - Test: TrainingRunWriter.end_run() updates ended_at, total_timesteps, model_path
    - Test: TrainingRunWriter.record_snapshot() writes win_rate_snapshots row
    - Test: Context manager on writer flushes on exit
  </behavior>
  <action>
    1. Install SB3 stack in the virtual environment:
       ```
       pip install "stable-baselines3[extra]>=2.8,<3.0" "sb3-contrib>=2.8,<3.0"
       ```
       This is a large install (~2GB with PyTorch). Verify with `python -c "from sb3_contrib import MaskablePPO; print('OK')"`.

    2. Update pyproject.toml to add RL optional dependency group (per D-01):
       ```toml
       [project.optional-dependencies]
       rl = ["stable-baselines3[extra]>=2.8,<3.0", "sb3-contrib>=2.8,<3.0"]
       dev = ["pytest>=8.0", "pytest-cov>=5.0", "mypy>=1.13", "ruff>=0.9"]
       ```
       Do NOT add torch directly -- it is pulled by SB3.

    3. Create `src/grid_tactics/db/__init__.py` exporting key classes.

    4. Create `src/grid_tactics/db/schema.py` with:
       - `SCHEMA_SQL` constant containing the full CREATE TABLE SQL from research (5 tables: training_runs, game_results, deck_compositions, card_actions, win_rate_snapshots) plus all indexes
       - `ensure_schema(db_path: Path) -> None` function that connects, sets WAL mode, executes SCHEMA_SQL
       - Use `CREATE TABLE IF NOT EXISTS` for idempotency
       - Tables per D-07, D-08, D-09: game results with winner/scores/deck/length/card_actions + training run metadata with hyperparameters/timestamps/win_rates

    5. Create `src/grid_tactics/db/writer.py` with:
       - `GameResultWriter` class:
         - `__init__(self, db_path: Path, buffer_size: int = 100)` -- calls ensure_schema
         - `record_game(self, run_id, episode_num, seed, winner, turn_count, p1_hp, p2_hp, p1_deck_hash=None, p2_deck_hash=None, game_duration_ms=None, training_player=None)` -- appends to buffer, auto-flushes at buffer_size
         - `flush(self)` -- batch INSERT via executemany
         - `__enter__`/`__exit__` context manager that flushes on exit
       - `TrainingRunWriter` class:
         - `start_run(self, run_id, hyperparameters: dict, description="") -> str` -- INSERT into training_runs
         - `end_run(self, run_id, total_timesteps, model_path=None)` -- UPDATE ended_at, total_timesteps, model_path
         - `record_snapshot(self, run_id, timestep, episode_num, win_rate_100, win_rate_1000=None, avg_game_length=None, avg_reward=None)` -- INSERT into win_rate_snapshots
         - `record_deck(self, deck_hash, card_counts: dict)` -- INSERT OR IGNORE into deck_compositions
         - `record_card_actions(self, game_id, player, card_numeric_id, times_played, total_damage=0, times_killed=0)` -- INSERT into card_actions
       - Per Pitfall 5: Use WAL mode, batch writes only, no per-step writes
       - Per Pitfall 6: Include `training_player` column in game_results INSERT

    6. Write tests in `tests/test_db.py` (TDD: write tests FIRST):
       - `test_schema_creation` -- ensure_schema creates all 5 tables
       - `test_wal_mode` -- PRAGMA journal_mode returns 'wal'
       - `test_game_result_roundtrip` -- write a game result, read it back with raw SQL
       - `test_batch_flush` -- write buffer_size-1 results (no flush), then one more (triggers flush), verify all in DB
       - `test_context_manager_flushes` -- with GameResultWriter: record games, exit, verify flushed
       - `test_training_metadata` -- start_run, end_run, verify row with start/end timestamps
       - `test_win_rate_snapshot` -- record_snapshot, verify queryable
       - `test_deck_composition` -- record_deck, verify card_counts JSON
       Use `tmp_path` pytest fixture for temporary SQLite databases.
  </action>
  <verify>
    <automated>cd "C:/Users/corma/CascadeProjects/card game" && python -c "from sb3_contrib import MaskablePPO; print('SB3 OK')" && pytest tests/test_db.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "sb3-contrib" pyproject.toml (RL dependency group exists)
    - grep -q "CREATE TABLE IF NOT EXISTS training_runs" src/grid_tactics/db/schema.py
    - grep -q "CREATE TABLE IF NOT EXISTS game_results" src/grid_tactics/db/schema.py
    - grep -q "CREATE TABLE IF NOT EXISTS win_rate_snapshots" src/grid_tactics/db/schema.py
    - grep -q "journal_mode=WAL" src/grid_tactics/db/schema.py OR grep -q "journal_mode" src/grid_tactics/db/schema.py
    - grep -q "class GameResultWriter" src/grid_tactics/db/writer.py
    - grep -q "class TrainingRunWriter" src/grid_tactics/db/writer.py
    - grep -q "executemany" src/grid_tactics/db/writer.py (batch insert)
    - grep -q "def flush" src/grid_tactics/db/writer.py
    - grep -q "test_schema_creation" tests/test_db.py
    - grep -q "test_game_result_roundtrip" tests/test_db.py
    - grep -q "test_batch_flush" tests/test_db.py
    - grep -q "test_training_metadata" tests/test_db.py
  </acceptance_criteria>
  <done>
    SB3 + sb3-contrib + PyTorch importable. SQLite schema creates 5 tables with WAL mode. GameResultWriter buffers and batch-inserts game results. TrainingRunWriter manages run lifecycle and win rate snapshots. All DB tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SQLite reader for dashboard queries</name>
  <read_first>
    - src/grid_tactics/db/schema.py (table definitions from Task 1)
    - src/grid_tactics/db/writer.py (writer API from Task 1)
    - .planning/phases/06-rl-training-pipeline/06-RESEARCH.md (dashboard-ready queries)
  </read_first>
  <files>
    src/grid_tactics/db/reader.py
    src/grid_tactics/db/__init__.py
    tests/test_db.py
  </files>
  <action>
    1. Create `src/grid_tactics/db/reader.py` with `GameResultReader` class (per D-09, dashboard-ready):
       - `__init__(self, db_path: Path)` -- read-only connection
       - `get_runs(self) -> list[dict]` -- all training runs sorted by started_at DESC
       - `get_run(self, run_id: str) -> dict | None` -- single run by ID
       - `get_game_results(self, run_id: str, limit: int = 1000) -> list[dict]` -- games for a run
       - `get_win_rate_over_time(self, run_id: str) -> list[dict]` -- win_rate_snapshots for a run, ordered by timestep
       - `get_card_usage(self, run_id: str) -> list[dict]` -- aggregated card_actions joined with game_results for a run: card_numeric_id, total_times_played, avg_times_played, games_with_card, win_rate_with_card
       - `get_overall_stats(self, run_id: str) -> dict` -- total games, win rate, avg game length, avg reward for a run
       - All methods return lists of dicts (dict factory on cursor) for easy DataFrame conversion in Phase 9
       - Connection uses `sqlite3.Row` or dict factory for column access

    2. Update `src/grid_tactics/db/__init__.py` to export GameResultReader.

    3. Add reader tests to `tests/test_db.py`:
       - `test_reader_get_runs` -- write 2 runs, verify get_runs returns both ordered by date
       - `test_reader_get_game_results` -- write run + 5 games, verify get_game_results returns all 5
       - `test_reader_win_rate_over_time` -- write snapshots, verify ordered by timestep
       - `test_reader_card_usage` -- write card_actions for games, verify aggregation
       - `test_reader_overall_stats` -- write run + games, verify totals
       Tests should use writer (Task 1) to populate data, then reader to query.
  </action>
  <verify>
    <automated>cd "C:/Users/corma/CascadeProjects/card game" && pytest tests/test_db.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class GameResultReader" src/grid_tactics/db/reader.py
    - grep -q "get_win_rate_over_time" src/grid_tactics/db/reader.py
    - grep -q "get_card_usage" src/grid_tactics/db/reader.py
    - grep -q "get_overall_stats" src/grid_tactics/db/reader.py
    - grep -q "test_reader_get_runs" tests/test_db.py
    - grep -q "test_reader_overall_stats" tests/test_db.py
    - grep -q "GameResultReader" src/grid_tactics/db/__init__.py
  </acceptance_criteria>
  <done>
    GameResultReader provides dashboard-ready queries for runs, game results, win rates over time, card usage aggregation, and overall stats. All reader tests pass using writer-populated data.
  </done>
</task>

</tasks>

<verification>
- `python -c "from sb3_contrib import MaskablePPO; print('OK')"` -- SB3 installed
- `python -c "import torch; print(torch.__version__)"` -- PyTorch installed
- `pytest tests/test_db.py -v` -- all DB tests pass
- `python -c "from grid_tactics.db import GameResultWriter, TrainingRunWriter, GameResultReader; print('imports OK')"` -- all DB classes importable
</verification>

<success_criteria>
- SB3/sb3-contrib/PyTorch are installed and importable
- SQLite schema creates 5 tables with WAL mode and dashboard-ready indexes
- GameResultWriter buffers writes and batch-inserts game results
- TrainingRunWriter manages training run lifecycle with metadata and snapshots
- GameResultReader provides query methods returning dicts for easy DataFrame conversion
- All DB tests pass (schema, roundtrip, batch, metadata, reader queries)
</success_criteria>

<output>
After completion, create `.planning/phases/06-rl-training-pipeline/06-01-SUMMARY.md`
</output>
