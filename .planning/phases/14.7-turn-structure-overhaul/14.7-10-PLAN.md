---
phase: 14.7-turn-structure-overhaul
plan: 10
type: execute
wave: 7
depends_on: ["14.7-01", "14.7-02", "14.7-03", "14.7-04", "14.7-05", "14.7-05b", "14.7-06", "14.7-07", "14.7-08", "14.7-09"]
files_modified:
  - tests/test_react_stack.py
  - tests/test_action_resolver.py
  - tests/test_legal_actions.py
  - tests/test_game_flow.py
  - tests/test_integration.py
  - tests/test_status_effects.py
  - tests/test_effect_resolver.py
  - tests/test_observation.py
  - tests/test_rl_env.py
  - tests/test_self_play.py
  - tests/server/test_events.py
  - tests/server/test_sandbox_session.py
  - tests/test_view_filter.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - data/GLOSSARY.md
autonomous: false
delivers_sc: [12]
delivers_requirements: [TURN-18]

must_haves:
  truths:
    - "All xfail marks added by plans 14.7-01 through 14.7-09 are resolved (test fixed, test deleted as obsolete, or explicitly reclassified as 'deferred to 14.8' with roadmap note)"
    - "pytest tests/ runs fully green — zero failures, zero unresolved xfails"
    - "No silent test-skips beyond the pre-existing conftest gates (torch / sb3 / flask_socketio availability)"
    - "TURN-01 through TURN-18 are added to REQUIREMENTS.md with traceability to Phase 14.7 complete"
    - "ROADMAP Phase 14.7 entry is marked [x] complete with commit date"
    - "STATE.md has Phase 14.7 closeout entry documenting all 10 plans + key decisions + tensor-parity-debt note"
    - "data/GLOSSARY.md finalized with Start: / End: / Summon: keyword entries"
    - "Sandbox save/load round-trip tested for EVERY new state field (react_context, react_return_phase, pending_trigger_queue_turn, pending_trigger_queue_other, pending_trigger_picker_idx, last_trigger_blip, extended ReactEntry fields)"
  artifacts:
    - path: "tests/ (multiple files)"
      provides: "All Phase 14.7-broken tests migrated to new resolution timing or reclassified"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TURN-01..TURN-18 added with 'Complete' status"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14.7 marked [x] with plan list 01..10 all checked"
    - path: ".planning/STATE.md"
      provides: "Phase 14.7 closeout entry with tensor-parity-debt amendment"
    - path: "data/GLOSSARY.md"
      provides: "Start:, End:, Summon: entries finalized"
  key_links:
    - from: ".planning/STATE.md"
      to: "tensor-parity-debt note"
      via: "append to Blockers/Concerns: 'RL checkpoints are now QUADRUPLY STALE after 14.7 — tensor engine is on hold, react_context / ReactContext / 4-phase TurnPhase / PendingTrigger queue not ported. Re-training required before tournament/eval.'"
      pattern: "QUADRUPLY STALE"
---

<objective>
Close Phase 14.7: migrate all broken tests, add TURN-01..TURN-18 to requirements, mark the phase complete in roadmap + state, update glossary, document tensor-parity debt. This is the final closeout plan — pattern-matches every prior 14.x closeout (14.1-05, 14.2-05, 14.3-05, 14.4-05, 14.5-07, 14.6-04).

Purpose: Throughout 14.7-01 through 14.7-09, tests were xfail'd when they broke due to resolution-timing changes, new phases, or new priority model. This plan drives all xfails to zero and adds the ~40 enumerated test-file migrations from research §7. After this plan, the test suite is green and Phase 14.7 is shippable.

Output:
- All xfail marks from 14.7-01..09 resolved
- All tests in the enumerated files updated to match the new resolution timing
- ~40+ test migrations expected across: test_react_stack.py, test_action_resolver.py, test_legal_actions.py, test_game_flow.py, test_integration.py, test_status_effects.py, test_effect_resolver.py, possibly test_observation.py / test_rl_env.py
- Sandbox save/load end-to-end test for every new state field
- REQUIREMENTS.md: TURN-01..TURN-18 added with 'Complete' status
- ROADMAP.md: Phase 14.7 marked [x]
- STATE.md: closeout entry + tensor-parity-debt amendment
- data/GLOSSARY.md: Start:, End:, Summon: keywords finalized
- Phase commit + roadmap-update commit
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@data/turn_structure_spec.md
@.planning/phases/14.7-turn-structure-overhaul/14.7-RESEARCH.md

@.planning/phases/14.7-turn-structure-overhaul/14.7-01-PLAN.md
@.planning/phases/14.7-turn-structure-overhaul/14.7-02-PLAN.md
@.planning/phases/14.7-turn-structure-overhaul/14.7-03-PLAN.md
@.planning/phases/14.7-turn-structure-overhaul/14.7-04-PLAN.md
@.planning/phases/14.7-turn-structure-overhaul/14.7-05-PLAN.md
@.planning/phases/14.7-turn-structure-overhaul/14.7-06-PLAN.md
@.planning/phases/14.7-turn-structure-overhaul/14.7-07-PLAN.md
@.planning/phases/14.7-turn-structure-overhaul/14.7-08-PLAN.md
@.planning/phases/14.7-turn-structure-overhaul/14.7-09-PLAN.md

@data/GLOSSARY.md
</context>

<scope_fence>
OUT OF SCOPE:
- Any new feature work
- Tensor engine parity (on hold)
- RL retraining
- Dashboard updates

This plan is PURE housekeeping + traceability. No game mechanics change. If any test migration reveals a genuine bug in the resolution timing, create a micro-plan 14.7-11 (not folded into this closeout).
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate all xfails + failures + migrate tests to new resolution timing</name>
  <files>
    tests/test_react_stack.py
    tests/test_action_resolver.py
    tests/test_legal_actions.py
    tests/test_game_flow.py
    tests/test_integration.py
    tests/test_status_effects.py
    tests/test_effect_resolver.py
    tests/test_observation.py
    tests/test_rl_env.py
    tests/test_self_play.py
    tests/server/test_events.py
    tests/server/test_sandbox_session.py
    tests/test_view_filter.py
  </files>
  <action>
Step 1 - Run the full test suite and enumerate current xfails + failures:

```bash
cd "D:/windsurf/card game" &amp;&amp; python -m pytest tests/ -q 2&gt;&amp;1 | tee /tmp/14.7-10-test-enum.txt
grep -E "XFAIL|FAILED|ERROR" /tmp/14.7-10-test-enum.txt | tee /tmp/14.7-10-broken.txt
```

The pre-enumeration below (done during planning via `pytest --collect-only` + grep of breaking patterns across the named test files) identifies roughly 30-45 tests across categories A-E that will need migration, well under the 60-test threshold that would trigger splitting into 14.7-10b. Cat F (RL/tensor) is xfail-all, ~15-25 tests.

Step 2 - Migrate by category using the concrete test IDs enumerated during planning:

**Category A: Immediate-effect timing from magic casts** (concrete: 2 tests enumerated + potential in e2e)

Tests that cast a magic card and assert damage/heal/etc happened RIGHT AWAY. Post-14.7-01, these tests need to drive PASS-PASS through the react window before asserting the effect.

Concrete test IDs (pre-enumerated via `pytest --collect-only`):
- `tests/test_action_resolver.py::TestPlayCardMagic::test_magic_card_resolves_effect_and_discards` - asserts `new_state.get_minion(0).current_health == 3` immediately after PLAY_CARD magic. MUST drive PASS-PASS first.
- `tests/test_action_resolver.py::TestCardMovementToGrave::test_magic_play_goes_to_grave` - asserts grave contents immediately after magic cast. Grave append timing MAY still be correct (cost-phase) but verify; likely migration = drive PASS-PASS.
- Potentially `tests/e2e/test_all_cards.py` tests that play magic cards and assert board state (scan for PLAY_CARD patterns where the card_def.card_type == MAGIC).

Migration pattern:
```python
# Before:
state = resolve_action(state, Action(action_type=PLAY_CARD, card_index=0, target_pos=(2, 3)), library)
assert state.minions[some_idx].current_health == expected  # expects immediate damage

# After:
state = resolve_action(state, Action(action_type=PLAY_CARD, card_index=0, target_pos=(2, 3)), library)
# 14.7-01: magic cast now defers to react stack. Drive PASS-PASS to resolve.
state = resolve_action(state, Action(action_type=PASS), library)  # opponent passes
state = resolve_action(state, Action(action_type=PASS), library)  # caster passes (closes window)
assert state.minions[some_idx].current_health == expected
```

Or use the advance_to_next_turn helper from 14.7-03 if the test doesn't care about window mechanics.

**Category B: PASSIVE / start-end trigger timing tests** (concrete: 4-6 tests)

Tests that verify Fallen Paladin heals at "next turn start", Emberplague applies burn, Dark Matter Battery damages opponent - post-14.7-03 these are now ON_START_OF_TURN / ON_END_OF_TURN with react windows in between. Migration: drive through the end-of-turn window and the next start-of-turn window before asserting.

Concrete test IDs:
- `tests/test_status_effects.py::test_burn_ticks_only_on_owners_turn_and_persists` - burn tick placement changed (now inside enter_start_of_turn not turn-flip tail). Verify assertion timing.
- `tests/test_status_effects.py::test_burn_lethal_kills_minion` - similar, burn-tick placement.
- `tests/test_ratchanter_aura.py::*` - any test asserting aura/passive timing (check each of the ~18 tests listed in this file; the ones that advance turns and assert buff state will need migration). Specifically scan for those using advance_to_next_turn equivalents or driving turn-flip.
- Any test asserting Fallen Paladin heal timing (search for fallen_paladin in tests/).
- Any test asserting Emberplague Rat burn-aura application timing.

**Category C: Death-order tests** (concrete: 4 tests)

Tests that assert a specific death-effect resolution order based on (active_first, instance_id). Post-14.7-05b (the _cleanup_dead_minions migration), death cleanup uses the priority queue. Migration: update tests to use TRIGGER_PICK flow OR verify the turn-first invariant still holds for single-trigger-per-queue auto-resolve cases.

Concrete test IDs:
- `tests/test_action_resolver.py::TestDeadMinionCleanup::test_on_death_effects_trigger_in_instance_id_order` - assertion of instance-id tiebreak; post-14.7-05b the tiebreak still applies WITHIN a player queue but the cross-player ordering is strict turn-first. Verify both invariants.
- `tests/test_action_resolver.py::TestDeadMinionCleanup::test_active_player_deaths_fire_before_opponent` - STILL CORRECT post-migration (turn-first preserves this). Regression test; should still pass.
- `tests/test_action_resolver.py::TestDeadMinionCleanup::test_ordering_tiebreak_by_instance_id` - verify.
- `tests/test_action_resolver.py::TestDeadMinionCleanup::test_chain_death_damage_kills_another_minion_with_on_death` - chain-reaction; post-14.7-05b this re-enqueues. Update to match new flow or verify behavior-equivalent.

**Category D: Melee chain tests** (concrete: 6-8 tests)

Tests that assert a single react window after the melee move+attack chain (14.1 model). Post-14.7-08 there are TWO separate windows. Migration: drive PASS-PASS twice + assert each window.

Concrete test IDs:
- `tests/test_action_resolver.py::TestPendingPostMoveAttack::test_melee_move_sets_pending_state_when_target_in_range` - post-14.7-08 also enters REACT; update to assert state.phase == REACT + pending set.
- `tests/test_action_resolver.py::TestPendingPostMoveAttack::test_pending_attack_resolves_combat_and_clears_state` - update to drive through both react windows.
- `tests/test_action_resolver.py::TestPendingPostMoveAttack::test_pending_decline_clears_state_with_one_react` - rename to test_pending_decline_skips_second_react; assert no phantom window after decline.
- `tests/test_action_resolver.py::TestPendingPostMoveAttack::test_pending_state_blocks_unrelated_actions` - verify block logic.
- `tests/test_legal_actions.py::TestPendingPostMoveAttackMask::test_pending_state_only_attack_and_decline` - legal actions during pending; verify still correct.
- `tests/test_legal_actions.py::TestPendingPostMoveAttackMask::test_pending_state_no_targets_only_decline` - same.
- `tests/test_legal_actions.py::TestPendingPostMoveAttackMask::test_build_action_mask_pending_state` - mask enumeration.
- Any `tests/test_integration.py` test that exercises melee move+attack flow (scan for uses of pending_post_move_attacker_id and MOVE->ATTACK sequences).
- `tests/test_tensor_engine_parity.py::TestPendingPostMoveAttack::*` (5 tests) - xfail all with reason "tensor engine on hold per CLAUDE.md; parity deferred to post-14.7" (stale debt, see Category F).

**Category E: test_integration.py + test_game_flow.py full-game tests** (concrete: 12-15 tests)

Break heavily due to deferred magic + new phases + trigger queue + two-window melee. Use advance_to_next_turn + the server auto-pass loop model as the guiding pattern.

Concrete test IDs (test_integration.py):
- `test_play_card_then_pass_react_advances_turn` - turn advance pattern changed (END_OF_TURN react window inserted).
- `test_pass_action_then_pass_react` - same.
- `test_draw_then_pass_react` - same.
- `test_react_counter_spell_buffs_minion` - timing of buff effect inside react window; verify.
- `test_react_with_surviving_minion` - verify.
- `test_multi_react_chain_lifo` - LIFO preserved; verify.
- `test_three_turns_state_consistency` - 3 full turns; heavy migration (now through START/END phases).
- `test_mana_deduction_and_regeneration` - mana regen location moved; verify turn_number > 2 regen still fires at expected boundary.
- `test_combat_damage_and_cleanup` - cleanup now goes through priority queue (14.7-05b).
- `test_legal_actions_valid_at_every_step` - extensive state-machine walk; migration needed.

Concrete test IDs (test_game_flow.py - server-side integration):
- `test_complete_game` - full game simulation, may need driver updates.
- `test_complete_game_both_receive_game_over` - verify.
- `test_complete_game_state_updates_throughout` - verify frame counts.
- `test_react_phase_opponent_gets_legal_actions` - react window opens correctly.

**Category F: test_observation.py / test_rl_env.py / test_self_play.py / test_tensor_engine_parity.py** (xfail as stale debt)

RL observations encode `phase` as a binary feature. Post-14.7-02 there are 4 phases. Observation encoding may need to widen. RESEARCH §7 marks this as "stale-debt, not blocker" - the pragmatic move is to xfail these with a reason pointing to "RL retraining / re-encoding deferred to post-14.7 cycle" AND add the same note to STATE.md. RL checkpoints are already triply stale per Blockers/Concerns; this is QUADRUPLY STALE now.

Concrete xfail targets (no migration - mark all with reason):
- All `tests/test_observation.py::*` tests that assert observation_space.shape or specific phase encoding.
- All `tests/test_rl_env.py::*` tests that depend on the old action space layout or phase encoding.
- All `tests/test_self_play.py::*` that use a trained checkpoint (the checkpoint predates 14.7 by multiple phases).
- All `tests/test_tensor_engine_parity.py::*` tests - tensor engine is on hold per CLAUDE.md; parity is by definition stale.

xfail reason string: `"14.7: RL checkpoints quadruply stale (14.1/14.2/14.5/14.7); tensor engine on hold per CLAUDE.md. Retraining required before revalidation."`

Use `@pytest.mark.xfail(strict=False, reason=...)` so if a test accidentally passes, it does not fail the suite but surfaces in `--runxfail` output.

For each broken test:
- Fix in-place if the migration is mechanical (drive PASS-PASS or similar)
- Leave as xfail with a reason IF it's legitimately deferred (RL retraining, tensor parity)
- Delete if the test is now conceptually obsolete (e.g. tests asserting a behavior that spec v2 explicitly overrides)

Step 3 — Iterate until zero fails and zero un-reasoned xfails:

```bash
python -m pytest tests/ -q 2>&1 | grep -E "(passed|failed|xfail|xpassed)"
```

Target end state: `N passed, M skipped, K xfailed (with reasons), 0 failed, 0 xpassed`.

Step 4 — Run strict mode to catch xpassed:

```bash
python -m pytest tests/ --runxfail -q 2>&1 | grep -E "failed|xpassed"
```

If any xfailed test has STARTED passing (xpassed), remove the xfail mark — the migration caught up to the test.

Step 5 — Add new end-to-end integration tests covering each TURN-* requirement that doesn't already have one:

- test_turn_banner_fires_on_turn_start (TURN-01) — state frame sends turn_number change; client would show banner (UI not tested here; just assert frame contents)
- test_three_phase_turn_order (TURN-02) — drive a full turn through START → ACTION → END and verify phase transitions
- test_melee_two_react_windows (TURN-06) — covered by 14.7-08 tests, consolidate here
- test_summon_compound_windows (TURN-07) — covered by 14.7-04, consolidate
- test_magic_deferred_until_stack_resolves (TURN-08) — covered by 14.7-01, consolidate
- test_simultaneous_priority_turn_first (TURN-12) — covered by 14.7-05, consolidate
- test_priority_picker_modal (TURN-13) — covered by 14.7-05, consolidate
- test_fizzle_invalid_target (TURN-14) — covered by 14.7-06, consolidate
- test_priority_applies_to_all_trigger_kinds (TURN-17) — covered by 14.7-05, consolidate

Consolidating means: either copy the test into test_integration.py under a `## 14.7 end-to-end` section, OR add a cross-reference in comments. User preference: keep tests organized by feature plan (less churn) with a clearly-commented integration section in test_integration.py.
  </action>
  <verify>
    cd "D:/windsurf/card game" &amp;&amp; python -m pytest tests/ -q &amp;&amp; python -m pytest tests/ --runxfail -q
  </verify>
  <done>
    - `pytest tests/` — zero failures, xfails all have reasons
    - `pytest tests/ --runxfail -q` — no xpassed tests
    - All Phase 14.7-specific integration tests consolidated in test_integration.py
    - Test summary: pre-14.7 count preserved or increased (no silent deletions without justification)
  </done>
</task>

<task type="auto">
  <name>Task 2: Sandbox save/load round-trip for every new state field</name>
  <files>
    tests/server/test_sandbox_session.py
    tests/test_game_state.py
  </files>
  <action>
Verify every new GameState field added in 14.7 round-trips correctly through SandboxSession's to_dict / from_dict. This protects Phase 14.6's save/load feature.

Add or verify these tests:

- test_sandbox_roundtrip_react_context: state with react_context=AFTER_SUMMON_DECLARATION → save → load → assert react_context preserved
- test_sandbox_roundtrip_react_return_phase: state with react_return_phase=TurnPhase.END_OF_TURN → round-trip
- test_sandbox_roundtrip_pending_trigger_queue: state with 2 entries in queue_turn + 1 in queue_other → round-trip
- test_sandbox_roundtrip_pending_trigger_picker_idx: state with picker open → round-trip
- test_sandbox_roundtrip_last_trigger_blip: state with blip dict set → round-trip
- test_sandbox_roundtrip_react_entry_originator: state with react_stack containing a summon_declaration originator → round-trip; ReactEntry.is_originator / origin_kind / effect_payload / source_minion_id / destroyed_attack / destroyed_dm all preserved
- test_sandbox_roundtrip_pending_post_move_in_react_phase: state in REACT with pending_post_move_attacker_id set (14.7-08 post-move window) → round-trip

For each test, create the state via sandbox API or direct GameState construction, call sandbox.save_state_dict() and sandbox.load_state_dict(), and assert equality on the relevant fields.
  </action>
  <verify>
    cd "D:/windsurf/card game" &amp;&amp; python -m pytest tests/server/test_sandbox_session.py tests/test_game_state.py -x -q
  </verify>
  <done>
    - All 7 sandbox round-trip tests pass
    - No field lost in serialization
    - Any new field discovered without serializer support triggers a fix in game_state.py + a matching test
  </done>
</task>

<task type="auto">
  <name>Task 3: Finalize REQUIREMENTS.md + ROADMAP.md + STATE.md + GLOSSARY.md + commit</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
    data/GLOSSARY.md
  </files>
  <action>
Step 1 — .planning/REQUIREMENTS.md. Add a new section for Turn Structure (after DEV- requirements):

```markdown
### Turn Structure & Priority (v1.1)

- [x] **TURN-01**: User sees a 1.2-1.8s `TURN X / PLAYER X` banner at the start of every turn (non-blocking) — _Completed 2026-04-18. See 14.7-09-SUMMARY.md._
- [x] **TURN-02**: Turn has three ordered phases (Start of Turn → Action → End of Turn); priority starts with the turn player in each phase — _Completed 2026-04-18. See 14.7-02-SUMMARY.md._
- [x] **TURN-03**: Start-of-turn booleans (burn tick) resolve before start-of-turn triggers — _Completed 2026-04-18. See 14.7-03-SUMMARY.md._
- [x] **TURN-04**: React window opens after start-of-turn triggers, before ACTION — _Completed 2026-04-18._
- [x] **TURN-05**: React window opens after every ACTION-phase action — _Preserved from pre-14.7; confirmed in 14.7-02._
- [x] **TURN-06**: Melee minions: up to 2 ACTION-phase actions (move + attack), each with own react window — _Completed 2026-04-18. See 14.7-08-SUMMARY.md. Supersedes Phase 14.1's combined-window decision._
- [x] **TURN-07**: Summon action opens two react windows: A on declaration (negate = refund), B on Summon: effect (negate = effect only) — _Completed 2026-04-18. See 14.7-04-SUMMARY.md._
- [x] **TURN-08**: Magic cast: costs resolve immediately; ON_PLAY as cast_mode originator at stack bottom; Prohibition above negates cast's effects — _Completed 2026-04-18. See 14.7-01-SUMMARY.md._
- [x] **TURN-09**: Reacts only fire when react_condition matches trigger — _Completed 2026-04-18. See 14.7-07-SUMMARY.md._
- [x] **TURN-10**: React window before END_OF_TURN completes, then turn passes — _Completed 2026-04-18._
- [x] **TURN-11**: React stack LIFO; each react opens new nested window until PASS-PASS or no legal react — _Completed 2026-04-18._
- [x] **TURN-12**: Simultaneous effects: turn-player's all resolve before non-turn-player's — _Completed 2026-04-18. See 14.7-05-SUMMARY.md._
- [x] **TURN-13**: Player with 2+ simultaneous triggers gets modal card picker — _Completed 2026-04-18. See 14.7-05-SUMMARY.md._
- [x] **TURN-14**: Effect with invalid target at resolve time fizzles (no error, no partial) — _Completed 2026-04-18. See 14.7-06-SUMMARY.md._
- [x] **TURN-15**: Start/end-of-turn triggered effects animate with center icon + source→effect→target blip; sequential — _Completed 2026-04-18. See 14.7-09-SUMMARY.md._
- [x] **TURN-16**: Spell-stage animation drives every react window — _Completed 2026-04-18._
- [x] **TURN-17**: Priority + fizzle apply uniformly to all trigger kinds — _Completed 2026-04-18._
- [x] **TURN-18**: All pre-existing tests pass or are explicitly updated; no silent regressions — _Completed 2026-04-18. See 14.7-10-SUMMARY.md._
```

Also update the Traceability section:
```markdown
| TURN-01 | Phase 14.7 | Complete |
... (18 entries)
```

And the coverage count:
```markdown
**Coverage (v1.1):**
- v1.1 requirements: 33 total (was 15; +18 TURN-*)
- Mapped to phases: 33
- Unmapped: 0
```

Step 2 — .planning/ROADMAP.md. Find Phase 14.7 entry (current line ~323). Change the checkbox from `[ ]` to `[x]` and add completion date:

```markdown
- [x] **Phase 14.7: Turn Structure Overhaul** ...
```

Update the plans list:
```markdown
**Plans:** 11/11 plans complete
Plans:
- [x] 14.7-01-PLAN.md -- Cast-mode originator + deferred magic ON_PLAY
- [x] 14.7-02-PLAN.md -- 3-phase turn model (START / ACTION / END + ReactContext)
- [x] 14.7-03-PLAN.md -- Start/End-of-turn triggered-effect pipeline + 9-card retag
- [x] 14.7-04-PLAN.md -- Compound react windows (Summon declaration + Summon: effect)
- [x] 14.7-05-PLAN.md -- Turn-player-first priority + same-player modal picker (start/end triggers)
- [x] 14.7-05b-PLAN.md -- Death-cleanup migration to priority queue (enables spec 4.3 worked example)
- [x] 14.7-06-PLAN.md -- Fizzle rule (target validity at resolve time)
- [x] 14.7-07-PLAN.md -- React-condition matching via ReactContext + 3 new conditions
- [x] 14.7-08-PLAN.md -- Melee two react windows (supersedes Phase 14.1 combined window)
- [x] 14.7-09-PLAN.md -- UI: turn banner + spell-stage generalization + trigger blips
- [x] 14.7-10-PLAN.md -- Test migration + traceability + closeout
```

Update the summary table at the bottom:
```markdown
| 14.7 Turn Structure Overhaul | v1.1 | 11/11 | Complete | 2026-04-18 |
```

Step 3 — .planning/STATE.md. Update:
- milestone position
- "Current Position" → "Phase 14.7 (turn-structure-overhaul) — COMPLETE (2026-04-18)"
- Add closeout entry with key decisions from all 10 plans (pattern-match prior 14.x closeouts — see STATE.md line 48-73 for 14.6 pattern).
- Update Blockers/Concerns:

```markdown
- Known issue: RL checkpoints are now QUADRUPLY STALE — (1) 14.1/14.2 encoding reinterpretations, (2) 8bd61e1 ACTION_SPACE_SIZE 1262→1287, (3) 14.5-02 tensor pile semantics, (4) 14.7 turn-structure overhaul: TurnPhase widened 2→4 phases (observation encoding needs re-widening), ReactContext added, PendingTrigger queue + TRIGGER_PICK/DECLINE_TRIGGER (actions 17/18) added, ReactEntry extended with 6 new fields, 3 new TriggerType values (ON_SUMMON/ON_START_OF_TURN/ON_END_OF_TURN), 3 new ReactCondition values. Tensor engine NOT updated — per CLAUDE.md it's on hold. Loadable as binary blobs but observations + action space + state shape all diverge silently. Full retraining required before tournament/eval.
```

- Progress: 0% → whatever the roadmap counter now reflects.

Step 4 — data/GLOSSARY.md. Verify Start: / End: / Summon: entries exist (added in 14.7-03). If anything is missing or unclear, finalize now. Also sync with src/grid_tactics/server/static/game.js KEYWORD_GLOSSARY.

Step 5 - Commit the plan changes:

```bash
cd "D:/windsurf/card game"
git add .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md data/GLOSSARY.md tests/ .planning/phases/14.7-turn-structure-overhaul/
git commit -m "docs(14.7): close out Phase 14.7 Turn Structure Overhaul

Phase 14.7 complete - 11/11 plans shipped.
- TURN-01..TURN-18 added to REQUIREMENTS.md
- ROADMAP Phase 14.7 marked [x]
- STATE.md closeout entry + QUADRUPLY STALE RL checkpoint note
- GLOSSARY + KEYWORD_GLOSSARY synced for Start: / End: / Summon:
- All ~40+ broken tests migrated, zero unreasoned xfails
- Zero silent regressions"
```

DO NOT push yet. The push is gated by the final checkpoint below (Task 4).
  </action>
  <verify>
    cd "D:/windsurf/card game" &amp;&amp; python -m pytest tests/ -q &amp;&amp; git log -1 --oneline &amp;&amp; cat .planning/STATE.md | head -20
  </verify>
  <done>
    - REQUIREMENTS.md has TURN-01..TURN-18 all marked Complete
    - ROADMAP Phase 14.7 marked [x] with 10/10
    - STATE.md has closeout entry + QUADRUPLY STALE debt note
    - GLOSSARY.md finalized
    - All changes committed locally (push gated by final checkpoint in Task 4)
    - Final `pytest tests/ -q` green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final pre-push approval gate</name>
  <what-built>
Phase 14.7 is fully complete locally:
- All 11 plans (14.7-01 through 14.7-10, including 14.7-05b) have shipped
- Test migration complete: zero unreasoned xfails, full `pytest tests/ -q` green
- REQUIREMENTS.md has TURN-01..TURN-18 all marked Complete
- ROADMAP Phase 14.7 marked [x] with 11/11
- STATE.md has closeout entry + QUADRUPLY STALE RL debt note
- GLOSSARY.md + KEYWORD_GLOSSARY synced
- Closeout commit exists locally on master

The NEXT step is `git push` to publish the entire 14.7 cycle to origin/master, which will trigger Railway deployment and make the changes user-visible.
  </what-built>
  <how-to-verify>
Before pushing, human confirms:

1. [ ] `python -m pytest tests/ -q` is fully green locally (no failures, xfails all have reasons)
2. [ ] `git log --oneline -15` shows the Phase 14.7 commit trail is clean
3. [ ] No accidentally-committed debug files, *.py temp patches, or local-only artifacts
4. [ ] `git diff origin/master..HEAD --stat` shows only expected files changed
5. [ ] Railway deployment is healthy / not in the middle of an unrelated issue

This is a 10-plan overhaul touching every turn in the game. Even though user memory says "always push", this is the closeout of the largest phase in 14.x. One human gate here is worth the friction - it catches accidental commits, uncommitted local work, or deployment windows where a push would be bad timing.

If approved: the executor runs `git push` immediately after.
  </how-to-verify>
  <resume-signal>Type "push approved" to push to origin/master. Type "hold" to defer the push. Describe any blocking issues for fixes.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Push to origin/master (gated by Task 4 approval)</name>
  <files>
    (no file edits)
  </files>
  <action>
ONLY RUN after Task 4 checkpoint returns "push approved".

```bash
cd "D:/windsurf/card game"
git push
```

Verify the push succeeded:

```bash
git log --oneline origin/master..HEAD
# Expected: empty output (local is now at or behind origin/master)

git log --oneline -5
# Expected: the closeout commit is visible and matches origin/master HEAD
```

If the push failed (upstream divergence, auth issue, hook rejection), diagnose and retry. Do NOT force-push to master unless the user explicitly requests it (per git safety protocol in CLAUDE agent doc).
  </action>
  <verify>
    cd "D:/windsurf/card game" &amp;&amp; git log --oneline origin/master..HEAD
  </verify>
  <done>
    - `git push` succeeded
    - `origin/master` matches local HEAD
    - Railway deployment starts (if auto-deploy enabled)
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` — zero failures, all xfails have reasons
- `python -m pytest tests/ --runxfail -q 2>&1 | grep -E "xpassed|failed"` — empty
- `git log --oneline -15` shows the Phase 14.7 commit trail (1 commit per plan + closeout)
- `grep -c "TURN-" .planning/REQUIREMENTS.md` — at least 18
- `grep "\\[x\\].*Phase 14.7" .planning/ROADMAP.md` — matches one line
- `grep "QUADRUPLY STALE" .planning/STATE.md` — matches one line
- `diff data/GLOSSARY.md <(grep -E "Start:|End:|Summon:" data/GLOSSARY.md)` — shows keyword entries present
</verification>

<success_criteria>
- Phase 14.7 shipped end-to-end
- Zero unreasoned xfails or failures
- All TURN-* requirements traceable to plans
- STATE.md + ROADMAP.md + REQUIREMENTS.md consistent
- Tensor parity debt documented
- SC12 satisfied (no silent regressions)
- TURN-18 satisfied
- Phase commit + push complete
</success_criteria>

<output>
After completion, create `.planning/phases/14.7-turn-structure-overhaul/14.7-10-SUMMARY.md` documenting:
- Total test migrations (count by category A-F)
- Final xfail list (each with a reason)
- Sandbox round-trip tests added (list by field)
- REQUIREMENTS.md section added (TURN-01..TURN-18)
- ROADMAP + STATE closeout status
- GLOSSARY sync status
- Commit trail (hash + message for each 14.7 plan)
- Any deferred work surfaced during migration (create micro-plan 14.7-11 if needed, else note "none")
</output>
