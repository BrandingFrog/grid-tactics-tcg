---
phase: 14.4-spectator-mode
plan: 05
type: execute
wave: 5
depends_on: ["14.4-04"]
files_modified:
  - tests/test_room_manager.py
  - tests/test_events.py
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
must_haves:
  truths:
    - "Test confirms join_as_spectator creates spectator with role='spectator'"
    - "Test confirms multiple spectators can join the same room"
    - "Test confirms mid-game spectator join works (room with active GameSession)"
    - "Test confirms submit_action from a spectator sid emits an error event"
    - "Test confirms state_update is emitted to spectator sids on every state change"
    - "ROADMAP.md and STATE.md reflect Phase 14.4 completion"
    - "Manual multi-tab smoke test passes (2 players + god spectator + non-god spectator)"
  artifacts:
    - path: "tests/test_room_manager.py"
      provides: "Spectator data model tests"
    - path: "tests/test_events.py"
      provides: "Spectator socket event tests"
  key_links:
    - from: "tests"
      to: "RoomManager + events.py"
      via: "spectator coverage"
---

<objective>
Lock in the feature with automated tests, run the manual multi-tab smoke test, and update planning docs.

Purpose: Prevents regression and confirms end-to-end behavior with real browser tabs.
Output: New test cases, updated STATE.md and ROADMAP.md, human-verified smoke test.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@tests/test_room_manager.py
@tests/test_events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add room_manager spectator tests</name>
  <files>tests/test_room_manager.py</files>
  <action>
  Add test functions:

  1. `test_join_as_spectator_basic`: create room with player, join as spectator, assert role=='spectator', token in get_spectator_tokens(code).
  2. `test_multiple_spectators_same_room`: join 3 spectators, assert all 3 tokens distinct and all in get_spectator_tokens.
  3. `test_spectator_join_after_game_started`: create room, both players join+ready, start_game, then join_as_spectator — must succeed and room lookup must still resolve.
  4. `test_spectator_join_when_room_full`: create room with 2 players, join spectator, must succeed (room "full" only refers to player slots).
  5. `test_remove_spectator`: join spectator, remove_spectator(token), assert token no longer in get_spectator_tokens and get_role(token) is None.
  6. `test_join_as_spectator_unknown_room`: assert ValueError raised.

  Use existing test fixtures / helpers in the file for CardLibrary loading.
  </action>
  <verify>pytest tests/test_room_manager.py -q</verify>
  <done>All new tests pass, no existing tests regress.</done>
</task>

<task type="auto">
  <name>Task 2: Add events.py spectator socket tests</name>
  <files>tests/test_events.py</files>
  <action>
  Use the existing flask-socketio test client pattern in test_events.py. Add tests:

  1. `test_spectate_room_event`: create room with one player, second client emits spectate_room, assert spectator_joined event received with session_token + room_code + god_mode.
  2. `test_spectator_receives_state_update`: full setup — 2 players in a started game, spectator joins, an action is submitted, assert spectator client received a state_update event.
  3. `test_spectator_submit_action_rejected`: spectator tries to emit submit_action, assert error event "Spectators cannot submit actions" received.
  4. `test_spectator_chat_allowed`: spectator emits chat_message, assert other clients in room receive it.
  5. `test_god_mode_spectator_sees_both_hands`: spectate with god_mode=True, parse received state_update payload, assert both players' hands are non-empty arrays (not just counts).
  6. `test_non_god_spectator_filtered`: spectate with god_mode=False, assert player 1's hand from spectator state matches player 0's filtered view (opponent hand is count only).

  Mock or use real game flow as appropriate to existing test conventions in this file.
  </action>
  <verify>pytest tests/test_events.py -q</verify>
  <done>All 6 new tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md and ROADMAP.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
  ROADMAP.md: Mark Phase 14.4 as complete in v1.1 milestone. Confirm it's listed AFTER 14.3. Update plan checkboxes to checked.

  STATE.md: Update "Current position" to reflect 14.4 done, add a Decisions entry: "Phase 14.4 spectator mode locked: god-mode optional, perspective fixed to Player 1 in non-god mode, mid-game join supported, multi-spectator per room supported." Add note that spectator perspective toggle is deferred.
  </action>
  <verify>grep -n "14.4" .planning/ROADMAP.md .planning/STATE.md</verify>
  <done>Both docs reflect 14.4 completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Multi-tab smoke test</name>
  <what-built>
  Full spectator mode end-to-end: lobby button + checkbox, server gating, view filter, dual-hand god view, SPECTATING badge, chat, mid-game join.
  </what-built>
  <how-to-verify>
  1. Start the server: `python -m grid_tactics.server.app` (or however the server is run today).
  2. Open 4 browser tabs to the local server URL.
  3. Tab A: create room as "Alice", note the room code.
  4. Tab B: join as "Bob" using the code, both ready up, game starts.
  5. Tab C: enter the same code, name "Eve", check God Mode, click Spectate Room.
     - Expected: Eve transitions to game screen, sees BOTH hands fully, sees SPECTATING (GOD MODE) badge, no Draw/Skip/Decline buttons, board is interactive-looking but clicking does nothing.
  6. Tab D: enter the same code, name "Dan", LEAVE God Mode unchecked, click Spectate Room.
     - Expected: Dan sees Player 1's view (Alice's hand visible, Bob's hand as count only), SPECTATING badge.
  7. Alice plays an action.
     - Expected: Both Eve and Dan see state update in real time.
  8. Eve sends a chat message ("hi from spectator").
     - Expected: Alice, Bob, and Dan all see it.
  9. Try clicking on a minion in Eve's view.
     - Expected: nothing happens, no errors in console.
  10. Open Tab E mid-game, spectate again — should work.
  11. Confirm console shows no errors in any tab.

  Type "approved" if all 11 checks pass, otherwise describe failures with which step.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- pytest tests/test_room_manager.py tests/test_events.py -q (all green)
- Manual smoke test approved
- ROADMAP and STATE updated
</verification>

<success_criteria>
Phase 14.4 fully shipped: tested, documented, human-verified in browser.
</success_criteria>

<output>
After completion, create `.planning/phases/14.4-spectator-mode/14.4-05-SUMMARY.md`
</output>
