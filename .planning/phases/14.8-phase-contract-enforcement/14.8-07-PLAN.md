---
phase: 14.8-phase-contract-enforcement
plan: 07
type: execute
wave: 10
depends_on: ["14.8-06"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - data/wire_format.md
  - VERSION.json
  - CHANGELOG.md
autonomous: true
delivers_sc: []
delivers_requirements: []

must_haves:
  truths:
    - "REQUIREMENTS.md confirms CONTRACT-01..08 are all marked [x] (delivered)"
    - "ROADMAP.md Phase 14.8 entry is updated: status flipped to 'Complete', completion date added, plans marked complete in the progress table at the bottom of the file (NOTE: 10 plans total now per the 14.8-03/04 splits — 01, 02, 03a, 03b, 04a, 04b, 04c, 05, 06, 07; if 02b or 05c spawned per budget gates, include those too)"
    - "STATE.md updated with the Phase 14.8 closeout summary, hook points for Phase 15, all 8 success criteria confirmed met"
    - "data/wire_format.md (NEW) documents the EngineEvent shape, all 19 event types, the contract_source taxonomy, the 5-category bypass for sandbox, and the per-viewer filter behavior — so future plans don't have to re-derive from research docs"
    - "VERSION.json bumped (minor or patch — match existing convention; check the file)"
    - "CHANGELOG.md entry added for v0.13.0 (or whatever the bumped version is) summarizing Phase 14.8: contract enforcement, event-stream wire format, single eventQueue, paladin-heal bug regression test"
    - "m2: All commits from plans 01-06 are pushed to master automatically WITHOUT asking the user (per project memory `feedback_always_push.md`); do NOT skip hooks (no --no-verify); Railway has auto-deployed (verify by checking Railway dashboard or curl-ing the deployed app version endpoint)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "All CONTRACT-* marked complete with completion date"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14.8 marked Complete with date; progress table updated for 10-plan split"
    - path: ".planning/STATE.md"
      provides: "Phase 14.8 closeout entry; hook points for Phase 15 (Resilience & Polish — reconnect can now use seq-based replay)"
    - path: "data/wire_format.md"
      provides: "Living documentation of the wire format that future plans reference"
      min_lines: 200
    - path: "VERSION.json"
      provides: "Bumped version reflecting completion"
    - path: "CHANGELOG.md"
      provides: "Phase 14.8 entry"
  key_links:
    - from: "ROADMAP.md Phase 14.8 progress entry"
      to: "REQUIREMENTS.md CONTRACT-* checklist"
      via: "all 8 marked complete"
      pattern: "CONTRACT-0[1-8].*\\[x\\]"
    - from: "data/wire_format.md"
      to: "engine_events.py + phase_contracts.py"
      via: "documents the public API surface so future code changes have a stable spec"
      pattern: "EngineEvent"
---

<objective>
Close out Phase 14.8: update all the planning docs (REQUIREMENTS, ROADMAP, STATE), bump VERSION, write a CHANGELOG entry, and produce data/wire_format.md as the long-term living spec for the EngineEvent + contract framework.

Per checker m2, push commits automatically without asking — that's the project convention (feedback_always_push.md).

Purpose: This is administrative cleanup. Phase 14.8 ships when all the docs reflect reality. Plan 15.x (Resilience & Polish) starts after this plan with reconnect-via-seq-replay as a teed-up option.

Output:
- REQUIREMENTS.md: 8 CONTRACT-* entries marked [x]
- ROADMAP.md: Phase 14.8 marked Complete; 10-plan progress table updated (01, 02, 03a, 03b, 04a, 04b, 04c, 05, 06, 07 — plus 02b/05c if budget gates triggered)
- STATE.md: closeout summary + hook points for 15.x
- data/wire_format.md: living spec doc
- VERSION.json bumped
- CHANGELOG.md entry
- Auto-push to master per m2
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14.8-phase-contract-enforcement/14.8-RESEARCH.md
@.planning/phases/14.8-phase-contract-enforcement/14.8-01-SUMMARY.md
@.planning/phases/14.8-phase-contract-enforcement/14.8-02-SUMMARY.md
@.planning/phases/14.8-phase-contract-enforcement/14.8-03a-SUMMARY.md
@.planning/phases/14.8-phase-contract-enforcement/14.8-03b-SUMMARY.md
@.planning/phases/14.8-phase-contract-enforcement/14.8-04a-SUMMARY.md
@.planning/phases/14.8-phase-contract-enforcement/14.8-04b-SUMMARY.md
@.planning/phases/14.8-phase-contract-enforcement/14.8-04c-SUMMARY.md
@.planning/phases/14.8-phase-contract-enforcement/14.8-05-SUMMARY.md
@.planning/phases/14.8-phase-contract-enforcement/14.8-06-SUMMARY.md

@VERSION.json
@CHANGELOG.md
@src/grid_tactics/engine_events.py
@src/grid_tactics/phase_contracts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md, ROADMAP.md, STATE.md; bump VERSION; add CHANGELOG entry</name>
  <files>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - VERSION.json
    - CHANGELOG.md
  </files>
  <action>
**REQUIREMENTS.md:**

Confirm all 8 CONTRACT-* entries are marked `[x]` with the completion date appended. If plan 05's REQUIREMENTS update was incomplete, finish it. Pattern follows existing entries (e.g., DEV-01..09 use `_Completed 2026-04-11_` parenthetical).

For example:
```
- [x] **CONTRACT-01**: Every state-mutating engine call declares a contract_source from one of four categories (trigger / status / action / system); sandbox edits use a fifth category (sandbox) that bypasses phase enforcement but still flows through the event stream — _Completed 2026-04-{date}_
```

Repeat for CONTRACT-02..08.

**ROADMAP.md:**

1. Find the Phase 14.8 entry in the bullet list near top (around line 179):
   - Change `- [ ] **Phase 14.8: ...**` to `- [x] **Phase 14.8: ...** (completed 2026-04-{date})`
2. Find the Phase 14.8 detail section (around line 355):
   - Update `**Plans**: TBD (target 5-8 plans)` to `**Plans:** 10/10 plans complete` (adjust for 02b / 05c if spawned)
   - Add a `Plans:` block listing each:
     ```
     Plans:
     - [x] 14.8-01-PLAN.md -- PHASE_CONTRACTS table + assertion + REQUIREMENTS CONTRACT-01..08
     - [x] 14.8-02-PLAN.md -- Pytest invariant test (every card × every phase × every action × every pending modal)
     - [x] 14.8-03a-PLAN.md -- engine_events module + EventStream collector + 19 event types + threading
     - [x] 14.8-03b-PLAN.md -- Server-side engine_events emit + sandbox refactor + view_filter + M3 next_event_seq
     - [x] 14.8-04a-PLAN.md -- Client eventQueue infrastructure + 10 simpler slot handlers
     - [x] 14.8-04b-PLAN.md -- 9 harder slot handlers + 4-gate deletion
     - [x] 14.8-04c-PLAN.md -- Visual UAT checkpoint (5 trickiest interaction chains)
     - [x] 14.8-05-PLAN.md -- Flip enforcement to strict; delete last_trigger_blip + PASSIVE + legacy paths
     - [x] 14.8-06-PLAN.md -- 5 visual UAT scenarios in data/tests/tests.json + Playwright runner + M1 react_window invariant
     - [x] 14.8-07-PLAN.md -- Closeout: REQUIREMENTS / ROADMAP / STATE / wire_format / version
     ```
   (Add 14.8-02b-PLAN.md and/or 14.8-05c-PLAN.md entries if those budget-gate splits were spawned in plans 02 or 05.)
3. Update the progress table at the bottom (around line 410):
   - Change `| 14.8 Phase Contract Enforcement | v1.1 | 0/TBD | Not started | - |` to `| 14.8 Phase Contract Enforcement | v1.1 | 10/10 | Complete | 2026-04-{date} |` (adjust the count if 02b/05c spawned)

**STATE.md:**

Append a new closeout block at the top, replacing the previous `stopped_at` text. Pattern follows the existing 14.7 entries — full closeout block with commits, behavioral changes, test posture, hooks for next phase. Be specific:

```markdown
stopped_at: "Phase 14.8 (Phase Contract Enforcement) SHIPPED ({date}). 10 plans, 10 waves. Architectural rewrite of the engine + wire format + client. ..."

## Phase 14.8 Plan 07 closeout ({date})

Phase 14.8 shipped as the final pre-release-15 plan. ...

Behavioral changes:
- ...

Engine changes (cumulative across plans 01-05):
- ...

Server / sandbox changes:
- ...

Client changes:
- ...

Test posture:
- 500+ existing tests pass under strict mode
- 80+ new tests across 4 new test files
- 5 Playwright UAT scenarios pass in headless + headed
- The original paladin-heal bug (paladin-heal-during-rat-react.md) now has a permanent regression test in scenario 2

Decisions (cumulative):
1. String-tagged contract sources (orchestrator decision #1) — confirmed correct in production
2. Soft OutOfPhaseError (orchestrator decision #2) — error event emitted, state preserved, session continues
3. react_window_opened on shortcut path (orchestrator decision #3) — symmetric event stream confirmed easier for client
4. Pending modal events for all 7 modal kinds (orchestrator decision #4) — uniform queue gating
5. Sandbox: 5th category (orchestrator decision #5) — clean separation; sandbox flows through events but bypasses phase check
6. Reconnect deferred to Phase 15 (orchestrator decision #6) — seq numbers are in place; replay handler is plan 15-NN's job
7. Auras out (orchestrator decision #7) — read-time only, no contract framework needed
8. pending_field as `requires_pending` not 6th category (orchestrator decision #8) — assertion checks pending first, then phase
9. ON_DEATH / ON_PLAY explicit phase enumeration (orchestrator decision #9) — wildcard forbidden by design
10. Feature flag staged migration (orchestrator decision #10) — off → shadow → strict over plans 01 → 04 → 05

Hook points for Phase 15:
- Reconnect: server keeps a per-session event log indexed by seq (plan 03b's M3 next_event_seq lays the groundwork). On reconnect, client sends `last_seq_seen`; server replays events from there. Initial snapshot via game_start; tail via engine_events.
- Game log sidebar (POLISH-02): consumes the same event stream; each event is a log entry. Filter for player-visible types (card_played, attack_resolved, minion_died, turn_flipped, etc.).
- Rematch (POLISH-03): tear down session, create new with same room code; emit fresh game_start.
```

**VERSION.json:**

Read current version. Bump per project convention (likely minor — Phase 14.8 is a major architectural change). If current is `0.12.x`, bump to `0.13.0`.

**CHANGELOG.md:**

Add entry at the top (or follow existing position-of-newest convention):

```markdown
## v0.13.0 — Phase 14.8: Phase Contract Enforcement (2026-04-{date})

**Architectural rewrite — engine + wire format + client.**

Engine:
- Every state-mutating call declares a `contract_source` (trigger / status / action / system / sandbox)
- Central PHASE_CONTRACTS table (src/grid_tactics/phase_contracts.py) maps source → allowed TurnPhase set
- `OutOfPhaseError` raised on violation in strict mode; rolled back + emitted as Socket.IO `error` event
- Default mode: shadow (logs without rejecting); CI runs strict
- 35+ contract sources tagged across effect_resolver, react_stack, action_resolver

Wire format:
- New `engine_events` Socket.IO event replaces post-resolution snapshot diff
- 19 EngineEvent types (minion_summoned / minion_died / minion_hp_change / ... / pending_modal_opened / fizzle / game_over)
- Each event tagged with contract_source + monotonic seq + animation_duration_ms
- Per-session next_event_seq field on Session and SandboxSession enables future reconnect-via-replay
- Same serializer for sandbox AND live PvP — sandbox cheats produce `sandbox:<verb>` events
- Per-viewer event filtering (card_drawn / card_discarded / pending_modal_opened)

Client:
- Single `eventQueue` replaces `_sandboxFrameQueue`, `_pendingPostStageFrame`, `_pendingTriggerBlip`, `_pendingTurnBanner`
- 19 slot handlers (one per event type) drive all animations
- DOM commits only after slot animation completes — no more snapshot-diff race conditions
- lastSeenSeq dedupe protects against out-of-order delivery

Removed:
- `GameState.last_trigger_blip` field (transient hack from 14.7-09)
- `TriggerType.PASSIVE` (unused since 14.7-03)
- `_fire_passive_effects` (dead code)
- `_enqueue_dead_minions_and_cleanup_zones` (legacy path superseded by 14.7-05b)
- 4 ad-hoc gate globals + 7 detect-* helpers from game.js (~500 line deletion)
- `state_update` / `sandbox_state` Socket.IO emits for non-initial frames (preserved for game_start / initial sandbox open)

Tests:
- 80+ new tests across `tests/test_phase_contracts.py`, `tests/test_phase_contract_invariants.py`, `tests/test_engine_events.py`, `tests/test_event_serialization.py`
- Phase contract invariant test scans every card × every phase × every action × every pending modal — fails if any tagging miss
- react_window open/close pair invariant catches future "shortcut" regressions
- 5 Playwright UAT scenarios in `data/tests/tests.json` + `tests/playwright/test_phase_14_8_uat.py`
- Original paladin-heal bug (paladin-heal-during-rat-react.md) now has a permanent regression test in scenario 2
- All 500+ pre-existing tests pass under strict mode

Documentation:
- New `data/wire_format.md` — living spec for EngineEvent + contract framework
- REQUIREMENTS.md: CONTRACT-01..08 added and delivered

Migration: This release is breaking for any client older than the bundled JS. The browser cache may need a hard reload after deploy.
```
  </action>
  <verify>
- `grep -c "CONTRACT-0.*\\[x\\]" .planning/REQUIREMENTS.md` returns 8
- `grep "Phase 14.8.*completed 2026" .planning/ROADMAP.md` returns at least one match
- `grep -c "Plan 07" .planning/STATE.md` returns >= 1
- `cat VERSION.json` shows bumped version
- `head -30 CHANGELOG.md` shows the new v0.13.0 entry at top
- `python -m json.tool VERSION.json > /dev/null` validates
- ROADMAP.md plans block lists 10 entries (01, 02, 03a, 03b, 04a, 04b, 04c, 05, 06, 07; plus 02b/05c if applicable)
  </verify>
  <done>
- REQUIREMENTS.md fully updated
- ROADMAP.md Phase 14.8 marked complete + plans listed (10-plan split honored) + progress table updated
- STATE.md closeout block added
- VERSION.json bumped
- CHANGELOG.md entry added
  </done>
</task>

<task type="auto">
  <name>Task 2: Write data/wire_format.md (living spec) + final commit + auto-push (m2)</name>
  <files>
    - data/wire_format.md (NEW)
  </files>
  <action>
**Create data/wire_format.md** — the long-term living specification for the wire format and contract framework. This document supersedes the research doc as the canonical reference; future plans (Phase 15.x reconnect, future card-system additions) read this file, not the research notes.

Structure (use markdown headings):

```markdown
# Grid Tactics Wire Format

> Phase 14.8 (2026-04-{date}). The canonical reference for the engine_events Socket.IO protocol and the phase-contract enforcement framework.

## Overview

[2 paragraphs: what the wire format is, why it exists, what problem it solved (paladin-heal bug), what it enables (Phase 15 reconnect / replay)]

## Contract Source Taxonomy

Five categories. Every state-mutating engine call must declare exactly one.

### trigger:<name>
[Definition + 11 trigger types with allowed phases — copied from PHASE_CONTRACTS]

### status:<name>
[Definition + status:burn (currently the only one) + extension pattern for future statuses]

### action:<name>
[Definition + 19 action types with allowed phases + the requires_pending mechanism for the 10 pending-modal actions]

### system:<name>
[Definition + 9 system events]

### sandbox:<verb>
[Definition + bypass mechanism + the 16 sandbox edit verbs]

## Phase Contract Assertion

[Pseudocode of assert_phase_contract — same logic as plan 01]

### Enforcement Modes

[off / shadow / strict — when to use each, default by environment]

### Soft Failure Semantics

[OutOfPhaseError catch in events.py → 'error' Socket.IO emit → state preserved → session continues — never crashes]

## EngineEvent

[Dataclass shape with field-by-field explanation, including seq's per-session monotonicity backed by Session.next_event_seq / SandboxSession._next_event_seq]

### Event Types

| Type | Triggered By | Slot Kind | Payload Fields |
|---|---|---|---|
| `minion_summoned` | action:play_card (deferred via originator) | summon | instance_id, card_id, position, owner |
| ... | (all 19 from plan 03a) |

### Animation Duration Defaults

[DEFAULT_DURATION_MS table]

## Per-Viewer Filtering

[card_drawn hides card_id for opponent unless god mode; pending_modal_opened hides options unless owner; ...]

## Socket.IO Protocol

### Server → Client

#### `engine_events`
```json
{
  "events": [<EngineEvent>, ...],
  "final_state": {<filtered GameState>},
  "legal_actions": [<ActionDescriptor>, ...]
}
```

[When emitted; back-compat note about state_update/sandbox_state still emitted on game_start / initial sandbox open]

#### `error`
```json
{
  "error_type": "phase_contract_violation",
  "contract_source": "trigger:on_play",
  "phase": "REACT",
  "allowed_phases": ["ACTION"],
  "pending_required": null,
  "message": "..."
}
```

#### `game_start`, `sandbox_state` (initial only)
[Snapshot shape preserved for fresh joiners]

### Client → Server

[Existing handlers documented for context: submit_action, sandbox_apply_action, sandbox_cheat_*, etc.]

## Client Event Queue

[Single queue + 19 slot handlers + drainEventQueue contract: shift one event, play it, commit DOM, drain next]

### Slot Blocking

[pending_modal_opened gates the queue until pending_modal_resolved arrives. Other events block for their animation_duration_ms.]

### Spell Stage Chain

[react_window_opened pushes; react_window_closed pops; LIFO chain semantics inherited from Phase 14.7-09]

### lastSeenSeq Deduplication

[Client tracks the highest seq seen; events with seq <= lastSeenSeq are skipped (logged as warning). On game_start, lastSeenSeq resets to -1.]

## Reconnect / Replay (Phase 15.x — Future)

[Sequence numbers are monotonic per session. On reconnect, client sends last_seq_seen; server replays events with seq > last_seq_seen from a per-session event log. Initial snapshot via game_start, tail via engine_events. NOT IMPLEMENTED in 14.8 per orchestrator decision #6. Foundation: Session.next_event_seq + SandboxSession._next_event_seq added in plan 03b.]

## Adding a New Contract Source

[Step-by-step: (1) add entry to PHASE_CONTRACTS in phase_contracts.py, (2) call assert_phase_contract at the mutation site, (3) emit a corresponding EngineEvent if applicable, (4) test_phase_contract_invariants.py auto-covers it, (5) document the new event type in this file]

## Adding a New Event Type

[Step-by-step: (1) add EVT_* constant in engine_events.py, (2) add to ALL_EVENT_TYPES + DEFAULT_DURATION_MS, (3) add to per-viewer filter if needed, (4) add a playEvent handler in game.js, (5) document here]

## Testing

[How to write a contract-aware test: use ViolationCapture, set strict mode, etc.]

## Known Edge Cases

[The 12 pitfalls from the research doc, as a TROUBLESHOOTING section]
```

Make it 200+ lines. The point is for the next developer (or AI) to be able to ADD a new card / new effect / new trigger / new event type WITHOUT rereading the research doc. The living spec consolidates everything.

**After writing, commit and push:**

Standard commit / push for the closeout. Use the project's normal commit message conventions:

```
docs(14.8-07): Phase 14.8 closeout — REQUIREMENTS / ROADMAP / STATE / wire_format / version

Phase 14.8 (Phase Contract Enforcement) shipped. 10 plans complete:
- 01: PHASE_CONTRACTS table + assertion
- 02: Pytest invariant test (cards × phases × actions × pending modals)
- 03a: engine_events module + EventStream + 19 event types
- 03b: Server-side engine_events emit + sandbox refactor + M3 next_event_seq
- 04a: Client eventQueue + 10 simpler slot handlers
- 04b: 9 harder slot handlers + 4-gate deletion
- 04c: Visual UAT checkpoint
- 05: Strict enforcement; legacy code deleted (last_trigger_blip / PASSIVE / etc.)
- 06: 5 visual UAT scenarios + Playwright runner + M1 react_window invariant
- 07: Closeout (this commit)

CONTRACT-01..08 delivered.
500+ pytest tests pass under strict mode.
5 UAT scenarios pass headed + headless.
Original paladin-heal bug now has permanent regression test.

VERSION 0.12.x → 0.13.0
```

**m2 — Auto-push WITHOUT asking the user, WITHOUT skipping hooks.**

Per project memory `feedback_always_push.md`: don't ask, just push after commit; Railway deploys from git. Run:

```bash
git push origin master
```

Do NOT use `--no-verify`. If a pre-commit hook fails, fix the underlying issue and create a NEW commit (do not amend or skip). Railway will auto-deploy on successful push.

After push, verify deployment:
```bash
sleep 60  # let Railway deploy
curl -s https://<railway-app-url>/version 2>/dev/null || echo "Manual check Railway dashboard"
```
  </action>
  <verify>
- `wc -l data/wire_format.md` returns >= 200
- `grep -c "EngineEvent" data/wire_format.md` returns >= 5
- `grep -c "contract_source" data/wire_format.md` returns >= 5
- `git log --oneline -1` shows the closeout commit
- `git status` shows clean working tree (everything committed)
- `git push origin master` succeeded (Railway auto-deploys) — confirmed by `git status` showing the branch up-to-date with origin/master
  </verify>
  <done>
- data/wire_format.md exists (200+ lines, comprehensive spec)
- All Phase 14.8 work committed and pushed (m2 auto-push without asking, hooks honored)
- Railway deployment confirmed (or manual verification scheduled)
- Phase 14.8 fully closed out
  </done>
</task>

</tasks>

<verification>
- All 8 CONTRACT-* requirements marked [x] in REQUIREMENTS.md
- ROADMAP.md Phase 14.8 marked Complete; progress table shows 10/10 (or includes 02b/05c if spawned)
- STATE.md has full closeout block
- VERSION.json bumped
- CHANGELOG.md has v0.13.0 entry
- data/wire_format.md exists as the canonical living spec (200+ lines)
- m2: All commits pushed to master automatically (no user prompt); Railway deployed
</verification>

<success_criteria>
- All planning docs reflect Phase 14.8 completion
- Living spec doc exists for future plans to reference
- Version + changelog updated per project convention
- m2: Final commit pushed to master without asking; Railway auto-deployed; production now running on the new architecture
</success_criteria>

<output>
After completion, create `.planning/phases/14.8-phase-contract-enforcement/14.8-07-SUMMARY.md`.

Key items:
- Confirmation that all 8 success criteria from ROADMAP Phase 14.8 are met
- Total LOC delta across the phase (added vs deleted)
- Test count delta (new tests added across the phase)
- Final commit hash + Railway deployed version
- Phase 15 readiness: hooks for reconnect (Session.next_event_seq foundation), game log, rematch
</output>
</content>
</invoke>