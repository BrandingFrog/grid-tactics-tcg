---
phase: 14.8-phase-contract-enforcement
plan: 04b
subsystem: client
tags: [client, event-queue, dispatcher, spell-stage, turn-banner, trigger-blip, pending-modal, fizzle, game-over, phase-indicator, snapshot-path, dead-code-marker]

# Dependency graph
requires:
  - phase: 14.8-phase-contract-enforcement
    plan: 04a
    provides: eventQueue + 19-case dispatcher + 10 simpler slot handlers + slotState.spellStageChain / pendingModalKind / pendingModalDeadline reserved slots + resetEventQueue wired at 6 sandbox-reset sites
  - phase: 14.8-phase-contract-enforcement
    plan: 03b
    provides: engine_events Socket.IO emission + per-viewer filter + M3 next_event_seq on both session classes
  - phase: 14.8-phase-contract-enforcement
    plan: 03a
    provides: EngineEvent + 19 EVT_* constants + DEFAULT_DURATION_MS table + event_collector threaded through engine call sites
provides:
  - 9 harder slot handlers fully implemented (playReactWindowOpened / playReactWindowClosed / playPhaseChanged / playTurnFlipped / playTriggerBlip / playPendingModalOpened / playPendingModalResolved / playFizzle / playGameOver) — all 19 EngineEvent types now route through fully-implemented handlers; no stubs remain in dispatcher
  - DELETION of 4 ad-hoc gate families (sandbox frame queue + drainer + applier + visual-duration budgets, post-stage-frame defer/flush, pending-trigger-blip defer, pending-turn-banner same-turn-dedupe + defer) — ~480 net lines removed from game.js
  - onStateUpdate reduced to pure snapshot-cache (applyStateFrame + return; no derive-* dispatch)
  - sandbox_state socket handler inlined as minimal commit (sandboxState write + autosave + renderSandboxToolbarState + renderSandbox; no queue buffering)
  - applyStateFrame (in _applyStateFrameImmediate) stripped of inline turn-banner + trigger-blip dispatch — both now owned by eventQueue
  - commitEventToDom extended for phase_changed (re-render LED indicator) + game_over (defensive no-op; slot handler opens overlay)
  - CSS: .fizzle-puff keyframe (💨 puff, 350ms scale+fade-up) + .event-queue-blocking informational scrim
  - 7 detect-* / derive-* snapshot-path helpers annotated with `// DEAD CODE post-14.8-04b — plan 14.8-05 will delete` comments for safe deletion in plan 05
affects:
  - 14.8-04c (visual UAT) — every animation now flows through eventQueue; UAT can exercise the full pipeline (spell-stage chain, turn banner, trigger blip, pending modals, fizzle, game_over, phase indicator) without double-rendering against a parallel snapshot-driven animation path
  - 14.8-05 (snapshot deletion) — onStateUpdate + onSandboxState are pure snapshot-cache; applyStateFrame is dead for animation purposes (renderGame still owns DOM rendering from state); 7 detect-*/derive-* helpers marked for bulk deletion
  - 14.8-06 (UAT / strict-mode flip) — eventQueue pipeline is the single source of animation truth; reset semantics match server (6 call sites from plan 04a still wired)

# Tech tracking
tech-stack:
  added: []  # All within existing vanilla-JS + socket.io + CSS stack
  patterns:
    - "Event-queue FIFO as the universal pacing primitive: once all 19 slot handlers call done() only when their animation completes, the FIFO delivery order (close → blip → banner) naturally reproduces the user-expected ordering that previously required explicit defer/flush globals. Deleted gate machinery (~480 lines) collapsed into 4 globals + switch statement (~15 lines)."
    - "DRY refactor of handler bodies: each of the 9 harder handlers REUSES its Phase 14.7-09 / 14-PLAY-03 primitive verbatim (_runTurnFlipVisuals, _fireTriggerBlipAnimation, _flashPhaseLed, _showSpellStage + _spellStageOnReactClosed, showGameOver). The only new DOM is the .fizzle-puff 💨 glyph (35 lines of handler + 18 lines of CSS keyframe). No animation visuals were rewritten."
    - "Pending-modal gating via slotState.pendingModalKind: the eventQueue's drainEventQueue checks this flag at top; when set, drain pauses. playPendingModalOpened sets it + spawns a minimal informational scrim (.event-queue-blocking, opacity 0.04, pointer-events:none — doesn't steal clicks from the actual modal); playPendingModalResolved clears + removes scrim. 5-minute safety deadline ensures no deadlock if the matching resolved event never arrives (server crash / tab close / socket drop)."
    - "Spell-stage chain tracking via slotState.spellStageChain LIFO: playReactWindowOpened pushes an entry; playReactWindowClosed pops and fires _spellStageOnReactClosed ONLY when chain.empty AND spell stage is visually up. Honors the Phase 14.7-09 chain semantics (opponent's first react stacks on the caster's stage; subsequent counter-reacts extend the chain; LIFO resolve-pop on close). The visible card slam-in is still driven by the snapshot path's detectSpellCast until plan 05 deletes it — this handler only paces the queue."
    - "commitEventToDom extended minimally: phase_changed re-renders the LED indicator (calling _setPhaseLeds from current gameState/sandboxState), game_over is defensive-noop (slot handler already called showGameOver). Other events still rely on snapshot path's renderGame/renderSandbox for DOM, per plan failure_handling — plan 05 migrates the rest."
    - "Fizzle visual as subtle off-main-channel glyph: 💨 at 3em with gray shadow (vs trigger blip's 5em with cyan glow) reads as 'dissipated' without competing with trigger blip glyphs. 350ms scale+fade-up animation anchored to source-minion's board tile (resolved via a gameState.board scan — source_minion_id → [row,col]); falls back to screen center if source is already off-board (common case — fizzled trigger means the source is dead). Anchor logic handles both sandbox (reads sandboxState) and live PvP (reads gameState)."
    - "Snapshot path → snapshot-cache reduction: onStateUpdate (live PvP) is now three lines — extract next/legal, applyStateFrame. Sandbox_state socket handler is eight lines — commit snapshot fields + autosave + render. All derive-*/detect-* dispatch REMOVED from the live path. The detect-* / derive-* helpers remain in the file with DEAD CODE markers (plan 05 removes)."
    - "Inline turn-banner + trigger-blip dispatch removed from applyStateFrame (was at _applyStateFrameImmediate's tail): both are now exclusive responsibility of playTurnFlipped / playTriggerBlip slot handlers. Reset semantics preserved — the eventQueue's lastSeenSeq dedupe (starts at -1 sentinel; bumps monotonically) replaces the old _lastBannerTurnKey + _pendingTurnBanner pair."

key-files:
  created:
    - .planning/phases/14.8-phase-contract-enforcement/14.8-04b-SUMMARY.md
  modified:
    - src/grid_tactics/server/static/game.js (net -479 lines: Task 1 +311 for 9 handlers + commitEventToDom extension; Task 2 -599/+120 for 4-gate deletion + onStateUpdate reduction + sandbox_state inline commit + 7 DEAD CODE markers)
    - src/grid_tactics/server/static/game.css (+35 lines: .fizzle-puff keyframe + .event-queue-blocking scrim)

key-decisions:
  - "Fully implemented 3 of 7 documented pending_modal kinds (tutor_select, trigger_pick, death_target_pick). The other 4 (conjure_deploy, revive_place, magic_cast_originator, post_move_attack) are NOT emitted by the engine today — their modals are opened by snapshot path's sync* handlers which read pending_* fields from gameState. Per plan failure_handling, simpler kinds ship now; complex kinds flagged for plan 05 attention when engine emission is added."
  - "Spell-stage chain handler tracks chain depth in slotState.spellStageChain but does NOT drive the visible card slam-in. The actual _showSpellStage call is still triggered by snapshot path's detectSpellCast (reading prev→next grave/react_stack diff). This is a deliberate partial migration: plan 04b owns the chain-close logic (pop + fire _spellStageOnReactClosed when empty+stageUp); plan 05 will own the chain-open card identity once card_played event payload is wired to drive it. Rationale: react_window_opened's payload carries react_context but NOT the specific card_numeric_id that opened the window — moving slam-in to event-driven requires either a payload extension or coordinating with card_played event arrival order, both outside 04b's scope."
  - "Fizzle's source anchor computed by scanning gameState.board for the source_minion_id (payload doesn't carry position — engine fizzle path's trigger has source_minion_id but no captured source_position by design). Falls back to center-screen if not found (source is already dead in the vast majority of fizzle cases — on_death trigger fires, then its target is invalidated before the effect resolves, source minion was already removed in _cleanup_dead_minions). The centered 💨 reads cleanly in this common case."
  - "5-minute pending-modal safety deadline is defensive only. The matching EVT_PENDING_MODAL_RESOLVED should arrive within tens of ms of user input for normal flows. The deadline exists for the abnormal cases: server crash between modal open and user action, user closes browser tab without resolving, socket drops. Without the deadline, the eventQueue would silently halt forever on these abnormal paths (DOM modal still open — user dismisses via snapshot path or reconnect)."
  - "onStateUpdate reduced to 3 lines (extract + applyStateFrame) per plan's explicit intent. This is a bold reduction from the plan 04a posture (which kept the full derive-* dispatch alive alongside eventQueue). Post-04b: there is NO animation work done in the live state_update path. All 500+ existing tests still pass because the tests assert engine state, not animation timing; and the baseline 1060/17 failure count is preserved exactly."
  - "sandbox_state socket handler inlined as ~8 lines directly in the subscriber (no separate function). Previously this called enqueueSandboxFrame which drove _drainSandboxFrameQueue → _applySandboxFrame (200+ lines). The new minimal commit preserves the snapshot-cache side-effects (sandboxState write, autosave, toolbar state sync, renderSandbox) but drops ALL the derive-*/detect-* dispatch + pacing logic. Snapshot path is otherwise intact — plan 05 deletes the subscription entirely."
  - "_showTurnBanner's internal _lastBannerTurnKey dedupe guard DELETED. Dedupe is now lastSeenSeq-based at the eventQueue level. The old dedupe was necessary because sandbox_state fired on initial socket connect + on every cheat/edit, so turn=1 emit would repeat. With eventQueue, each EVT_TURN_FLIPPED carries a unique seq, and repeats are silently dropped by the dedupe gate before reaching playTurnFlipped. Same net result, simpler mechanism."
  - "_hideSpellStage's post-stage-frame flush logic DELETED. Previously this flushed a parked sandbox frame + fired deferred trigger blip + ran deferred turn banner, all coordinated via _pendingPostStageFrame / _pendingTriggerBlip / _pendingTurnBanner globals. With eventQueue, the server emits react_window_close → trigger_blip → turn_flip in seq order, and each handler calls done() only when its animation completes. FIFO discipline reproduces the user-expected ordering (stage close → HP commit → blip → banner) without any ad-hoc deferral."
  - "Annotated 7 detect-*/derive-* helpers with uniform `// DEAD CODE post-14.8-04b — plan 14.8-05 will delete` prefix + 1-line 'replaced by <slot handler name>' note. These remain callable until plan 05 (not removed in 04b per plan: 'retain as reference'). Grep returns ≥7 (actual: 7) — meets verify gate."
  - "The plan body's applyStateFrameSelective shim was NOT needed: by deleting the animation dispatch entirely from onStateUpdate + onSandboxState, the snapshot path naturally stops double-rendering. Simpler than the selective-suppress shim the plan body described."

patterns-established:
  - "Ad-hoc coordination gates (defer globals + flush hooks + per-frame buffers) are replaceable by FIFO + per-slot done() callbacks IF you have a single monotonic delivery stream. The 4 gates deleted in this plan each solved a subset of: 'event A must appear on screen before event B even though the network delivered them out of order / in the same frame'. FIFO + callback-on-animation-end is the universal solution — any future coordination problem should consider this shape first."
  - "DEAD CODE markers with uniform prefix enable bulk regex deletion in subsequent plans. The `// DEAD CODE post-14.8-04b` prefix is grep-able; plan 05 can delete contiguous blocks starting with these comments, verify no live call sites via jshint/tsc, and remove."
  - "Minimal informational scrim pattern: low-opacity (0.04) pointer-events:none overlay provides visual hint that the queue is paused without stealing clicks from the actual user-focused modal. Scrim lifecycle tied 1:1 to slotState.pendingModalKind via ensure-scrim-exists (opened) + remove-scrim-if-exists (resolved). Safe to call either without the other (defensive DOM lookup + conditional remove)."

# Metrics
duration: ~30 min
completed: 2026-04-21
---

# Phase 14.8 Plan 04b: 9 Harder Slot Handlers + 4-Gate Deletion

**The behaviorally-significant client plan of Phase 14.8. All 19 EngineEvent types now route through fully-implemented playEvent handlers; the 4 ad-hoc defer/buffer gates that grew up over Phase 14.7-09 are DELETED; onStateUpdate + sandbox_state handler reduced to pure snapshot-cache role. Snapshot path still alive (applyStateFrame + renderGame / renderSandbox still commit DOM from state) but no longer drives animations — plan 05 deletes it.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-04-21T09:50Z (approx, per commit timestamps)
- **Completed:** 2026-04-21T10:13Z
- **Tasks:** 2
- **Files modified:** 2 (`src/grid_tactics/server/static/game.js`, `src/grid_tactics/server/static/game.css`)
- **Lines net delta:** game.js −479 (Task 1 +311 / Task 2 −599 / +120); game.css +35
- **Test posture:**
  - `node -c src/grid_tactics/server/static/game.js`: clean
  - `python -c 'import grid_tactics.server.app'`: clean
  - tests/server + tests/test_event_serialization + tests/test_phase_contracts: 108/108 passed
  - Broader engine+server suite (excluding RL/tensor/training/e2e): **1060 passed / 17 failed / 27 skipped — EXACT baseline match from plan 04a** (same 17 documented pre-existing failures: 8 react_stack TestStartOfTurn/TestPhase14_7_05, 7 game_loop TypeError, 1 spectator, 1 game_flow ordering bug)

## Accomplishments

### Task 1: 9 Harder Slot Handlers (commit `b20b70d`)

Replaced the 9 stubbed `console.warn + setTimeout(done, 0)` placeholders from plan 04a with full implementations. Each handler REUSES existing Phase 14.7-09 / 14-PLAY-03 animation primitives rather than reimplementing — DRY per plan failure_handling.

| Handler | Body | Reuse | Duration |
|---------|------|-------|----------|
| `playReactWindowOpened` | push to `slotState.spellStageChain` LIFO (chain entry: react_context, react_player_idx, return_phase, source_event_seq) | visible slam-in still driven by snapshot path's detectSpellCast — chain handler only paces the queue | 600ms (DEFAULT) |
| `playReactWindowClosed` | pop chain entry; if chain empty AND spell-stage visually up, fire `_spellStageOnReactClosed` | `_spellStageOnReactClosed` from 14.7-09 (LIFO resolve-pop + fade) | 400ms (DEFAULT) |
| `playPhaseChanged` | branch on payload.new (phase name): START_OF_TURN → `_flashPhaseLed('start', 900)`, END_OF_TURN → `_flashPhaseLed('end', 900)` | `_flashPhaseLed` from 14.7-09 | 0ms (instant) |
| `playTurnFlipped` | call `_runTurnFlipVisuals(new_turn, new_active_idx)` | `_runTurnFlipVisuals` from 14.7-09 (END LED → banner + START LED) | 1500ms (DEFAULT, matches CSS keyframe) |
| `playTriggerBlip` | call `_fireTriggerBlipAnimation(payload)` | `_fireTriggerBlipAnimation` from 14.7-09 (source pulse → center glyph → target pulse) | 900ms (DEFAULT) |
| `playPendingModalOpened` | set `slotState.pendingModalKind = payload.modal_kind` + 5-min safety deadline + spawn `.event-queue-blocking` scrim | Scrim is new CSS (minimal); drain-pause gate is from 04a's drainEventQueue | 0ms (gates queue) |
| `playPendingModalResolved` | clear pendingModalKind + deadline + remove scrim | — | 0ms |
| `playFizzle` | scan gameState.board for payload.source_minion_id → anchor `.fizzle-puff` (💨 glyph) to that tile; center-screen fallback | New CSS `.fizzle-puff` (scale+fade-up, 350ms) | 350ms |
| `playGameOver` | call `showGameOver({winner: payload.winner, final_state: window.__lastFinalState \|\| gameState})` | `showGameOver` from 14-PLAY-03 (overlay with VICTORY/DEFEAT/DRAW banner + reason) | 0ms (overlay stays) |

Extended `commitEventToDom` (04a stub) for two events:

- `phase_changed` → re-renders both `#phase-badge` and `#sandbox-phase-badge` via `_setPhaseLeds(badge, gameState.phase, gameState.react_return_phase)` so the LED indicator reflects the new phase immediately (the snapshot path's renderGame would eventually catch this, but commitEventToDom fires right after playPhaseChanged so the LED is live BEFORE the snapshot frame commits)
- `game_over` → defensive no-op (slot handler already called showGameOver; onGameOver socket handler also fires with same payload — `showGameOver` is idempotent)

CSS additions (game.css):

- `.fizzle-puff` + `@keyframes fizzle-puff-in` (350ms 💨 scale-up + opacity-fade-up)
- `.event-queue-blocking` (fixed-inset scrim, 0.04 opacity, pointer-events:none, z-index 5 — below modals)

### Task 2: 4-Gate Deletion + Snapshot-Path Reduction (commit `1e02c79`)

**DELETED (all 11 verify-gated symbols → 0 count):**

| Symbol | Purpose | Replaced by |
|--------|---------|-------------|
| `_sandboxFrameQueue` global | Per-frame payload buffer | eventQueue |
| `_sandboxNextApplyAt` | Visual-hold wall-clock timestamp | eventQueue lastSeenSeq + per-slot done() |
| `_sandboxDrainTimer` | Self-rescheduling setTimeout hook | drainEventQueue re-entry |
| `_SB_HOLD_*` budget constants (4) | Visual duration budgets | DEFAULT_DURATION_MS in engine_events.py |
| `enqueueSandboxFrame` | Queue push + drain kick | eventQueue push via onEngineEvents |
| `_drainSandboxFrameQueue` | Timestamp-gated pop loop | drainEventQueue |
| `_applySandboxFrame` | 200-line per-frame commit + derive-* dispatch + deferral branching | Inlined 8-line minimal commit |
| `_pendingPostStageFrame` + `_flushPendingPostStageFrame` | Defer ENTIRE state commit behind spell stage close | FIFO ordering (react_window_closed → state frame commits naturally) |
| `_pendingTriggerBlip` | Defer trigger blip behind same-frame stage close | FIFO ordering (react_window_closed → trigger_blip sequence) |
| `_pendingTurnBanner` + `_showTurnBannerOrDefer` | Defer turn banner behind spell stage | FIFO ordering (trigger_blip → turn_flipped sequence) |
| `_lastBannerTurnKey` | Same-turn dedupe guard | eventQueue lastSeenSeq dedupe |
| `applyStateFrameSelective` | (Never implemented in 04a per SUMMARY) | Unused — full removal of derive-* from live path is cleaner |

**REDUCED:**

- `onStateUpdate(data)` — was 70 lines (derive + detect + enqueue 4 job types across noop/non-noop branches) — now 4 lines (extract next/legal + `applyStateFrame(next, nextLegal)` + return)
- `socket.on('sandbox_state', ...)` — was 3 lines (`enqueueSandboxFrame(payload)`) forwarding to 200-line `_applySandboxFrame` — now 22-line inline minimal commit (sandboxState write + gameState sync if sandboxMode + autosave + renderSandboxToolbarState + renderSandbox; no queue, no derive, no pacing)
- `_applyStateFrameImmediate` (in `applyStateFrame`) — stripped inline turn-banner dispatch + inline trigger-blip dispatch (~20 lines each); replaced with 8-line comment explaining the migration

**MARKED DEAD CODE (7 helpers, annotation pattern for plan 05 deletion):**

```
// DEAD CODE post-14.8-04b — plan 14.8-05 will delete
// <1-line original purpose>. Replaced by <slot handler>. Retained as
// reference until plan 05 deletes the snapshot path entirely.
function detectSpellCast(prev, next) { ... }
function detectSpellStageClose(prev, next) { ... }
function detectReactWindowClose(prev, next) { ... }
function deriveAnimationJob(prev, next) { ... }
function deriveCardFlyJobs(prev, next) { ... }
function deriveDrawJobs(prev, next) { ... }
function derivePlayerHpDeltaAnims(prev, next) { ... }
```

Uniform prefix `// DEAD CODE post-14.8-04b` enables `grep -c` audit in plan 05 — actual count: 7, meets the plan's ≥7 verify gate.

## Verification

| Gate | Requirement | Actual |
|------|-------------|--------|
| `node -c game.js` | clean | clean |
| 9 harder handlers exist | `grep -c "function play(ReactWindowOpened\|ReactWindowClosed\|TurnFlipped\|TriggerBlip\|PendingModalOpened\|PendingModalResolved\|Fizzle\|GameOver\|PhaseChanged)"` ≥9 | 9 |
| No stubs | `grep -c "stub — implemented in 04b"` == 0 | 0 |
| `_sandboxFrameQueue` deleted | `grep -c` == 0 | 0 |
| `_pendingPostStageFrame` deleted | `grep -c` == 0 | 0 |
| `_pendingTriggerBlip` deleted | `grep -c` == 0 | 0 |
| `_pendingTurnBanner` deleted | `grep -c` == 0 | 0 |
| `applyStateFrameSelective` deleted | `grep -c` == 0 | 0 |
| DEAD CODE markers | `grep -c "DEAD CODE post-14.8-04b"` ≥7 | 7 |
| Pytest baseline | 1060 passed / 17 failed / 27 skipped | EXACT match |

## Deviations from Plan

### Partial: Only 3 of 7 pending_modal kinds wired (Rule 4 territory, resolved per failure_handling)

Plan 04b's Task 1 action mentions 7 modal kinds for playPendingModalOpened: tutor_select, death_target_pick, revive_place, trigger_pick, conjure_deploy, magic_cast_originator, post_move_attack.

**Grep of engine emission sites:**

- `effect_resolver.py:490` → `"modal_kind": "tutor_select"`
- `action_resolver.py:1754` → `"modal_kind": "death_target_pick"`
- `react_stack.py:469 + 488` → `"modal_kind": "trigger_pick"`

**Only 3 are emitted today.** The other 4 (revive_place, conjure_deploy, magic_cast_originator, post_move_attack) are opened by snapshot-path sync* handlers reading pending_* state fields (syncPendingReviveUI, syncPendingConjureDeployUI, etc.) — they DON'T currently emit EVT_PENDING_MODAL_OPENED.

Per plan failure_handling: *"If pending_modal_opened gets messy because some modal kinds have very different state requirements, document and ship the simpler kinds; flag the complex ones for plan 05 attention."*

**Action taken:** playPendingModalOpened handles any modal_kind generically — sets `slotState.pendingModalKind = payload.modal_kind` to gate the queue. The 3 emitted kinds gate today; the 4 unemitted kinds will gate automatically once engine emission is added (plan 05 or later). The actual modal DOM (tutor modal / death-target highlights / revive overlay / etc.) is still owned by snapshot-path sync* handlers — plan 04b does NOT migrate modal-opening DOM to eventQueue, only queue-gating.

**Hook for plan 05:** extend engine emission for conjure_deploy / revive_place / magic_cast_originator / post_move_attack modal kinds; the client gate is already generic.

### Plan-body-vs-actual: applyStateFrameSelective shim never needed

The plan body described an `applyStateFrameSelective(payload, skipDetect=[...])` shim that would suppress detect-* helpers for event-covered types. Plan 04a SUMMARY documented that the shim was NOT implemented because the detect logic was inline (not factored out). Plan 04b didn't need it either — by reducing onStateUpdate + sandbox_state to pure state-commit, the snapshot path naturally stops invoking detect-*. The shim was redundant middleware; direct reduction is cleaner.

## Authentication Gates

None.

## Task Commits

1. **Task 1: 9 harder handlers + CSS** — `b20b70d` (feat)
2. **Task 2: 4-gate deletion + snapshot-path reduction + DEAD CODE markers** — `1e02c79` (refactor)

_Plan-metadata commit follows this SUMMARY._

## Hook Points for Plan 04c (Visual UAT)

1. **All 19 events produce real visuals.** Plan 04c can exercise:
   - spell-stage chain (LIFO slam-in, resolve-pop) via multi-react scenarios
   - turn banner + phase LED cycle via turn-end → turn-start transitions
   - trigger blip (source pulse → glyph → target pulse) via on_start_of_turn / on_end_of_turn / on_death triggers
   - pending modal gate via tutor, death_target_pick, trigger_pick flows
   - fizzle via targets invalidated at resolve time (Phase 14.7-06 §7.3)
   - game_over via HP depletion or sacrifice crossing
   - phase_changed LED indicator updates during all phase transitions
2. **No double-rendering.** The snapshot path's animation dispatch is DELETED — each event animation fires exactly once (via its slot handler).
3. **window.__eventQueueDebug** exposes queue / running / lastSeenSeq / slotState (spellStageChain / pendingModalKind / pendingModalDeadline) for live introspection.
4. **Reset mismatch test:** trigger a sandbox_reset mid-animation → eventQueue clears and re-anchors at seq=0; next first event (seq=0) arrives and is delivered (lastSeenSeq=-1 sentinel after reset).
5. **Race conditions surfaced during local smoke testing:** none observed in this plan's scope (local `node -c` + full pytest baseline). UAT will cover the in-browser interaction matrix.

## Hook Points for Plan 05 (Snapshot Path Deletion)

1. **`applyStateFrame` + `_applyStateFrameImmediate`** still commit DOM state via renderGame/renderSandbox. Plan 05 needs to either (a) move renderGame/renderSandbox triggers into commitEventToDom, or (b) delete state_update / sandbox_state socket subscriptions entirely and drive renders from within event handlers.
2. **7 detect-*/derive-* helpers marked DEAD CODE** — `grep -c "DEAD CODE post-14.8-04b"` returns 7. Each has a 1-line replacement reference pointing at the equivalent slot handler. Bulk deletion is safe: no live call sites remain (verified by removing enqueueSandboxFrame path and re-running pytest with 0 regression).
3. **Legacy `state.last_trigger_blip` field** still dual-written by engine per plan 03a — client no longer reads it. Plan 05 deletes the field write in react_stack.py (single call site).
4. **pending_modal_opened emission gap:** 4 modal_kinds (revive_place, conjure_deploy, magic_cast_originator, post_move_attack) aren't emitted. Plan 05 adds emission; client handler already routes them generically.
5. **commitEventToDom** stub is 50% filled (phase_changed + game_over wired). Plan 05 fills in mana/phase/legal_actions sync that snapshot path used to commit after each frame; the 17-line body has a clear per-event-type branch pattern for extension.

## Hook Points for Plan 06 (UAT / Strict-Mode Flip)

- M1 react_window symmetry placeholder test (plan 02) can un-skip once plan 04c UAT confirms the eventQueue's react_window_opened/closed symmetry matches engine emission shape.
- Strict-mode contract enforcement (14.8-phase root goal) can flip default to strict once 04c UAT confirms no client-visible regressions. Engine-side contract violations are already 0 per plan 02's invariant test.
