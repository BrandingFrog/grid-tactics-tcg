---
phase: 14.2-tutor-choice-prompt
plan: 03
type: execute
wave: 3
depends_on: ["14.2-02"]
files_modified:
  - src/grid_tactics/legal_actions.py
  - src/grid_tactics/tensor_engine/legal_actions.py
  - tests/test_legal_actions.py
autonomous: true
must_haves:
  truths:
    - "When pending_tutor is set, legal actions are restricted to: PLAY_CARD slots [0:len(matches)] (reinterpreted as TUTOR_SELECT) and slot 1001 (reinterpreted as DECLINE_TUTOR). Every other slot is masked out."
    - "When pending_tutor is NOT set and pending_post_move_attacker is also not set, legal actions are unchanged from 14.1 behavior"
    - "Python and tensor legal-action masks agree on tutor pending states"
    - "Parity with 14.1: if neither pending flavor active, slot 1001 still means PASS; legal mask disambiguates by inspecting pending fields"
  artifacts:
    - path: src/grid_tactics/legal_actions.py
      provides: "Mask-generation code path for pending_tutor state"
    - path: src/grid_tactics/tensor_engine/legal_actions.py
      provides: "Vectorized equivalent for [N] games"
    - path: tests/test_legal_actions.py
      provides: "Tests: pending tutor allows only select/decline; non-pending unchanged; mask parity with 14.1 melee pending; PLAY_CARD slots gated by len(matches)"
  key_links:
    - from: src/grid_tactics/legal_actions.py::compute_legal_actions
      to: src/grid_tactics/game_state.py::pending_tutor_matches
      via: "Branch on pending_tutor_player_idx; emit mask[0:len(matches)] and mask[1001]=True"
      pattern: "pending_tutor_player_idx"
---

<objective>
Gate the action-space mask on pending_tutor in both engines. Enforce the slot-reinterpretation decision from Wave 1.

Purpose: Keep agents (human and RL) from selecting illegal actions while pending_tutor is set.
Output: Updated legal_actions (Python + tensor) + tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14.2-tutor-choice-prompt/14.2-01-PLAN.md
@.planning/phases/14.2-tutor-choice-prompt/14.2-02-PLAN.md
@.planning/phases/14.1-melee-move-and-attack/14.1-03-SUMMARY.md
@src/grid_tactics/legal_actions.py
@src/grid_tactics/tensor_engine/legal_actions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Python legal_actions pending_tutor branch</name>
  <files>src/grid_tactics/legal_actions.py</files>
  <action>
1. At the top of `compute_legal_actions` (or equivalent), BEFORE the existing 14.1 pending_post_move_attacker branch, add:
   ```python
   if state.pending_tutor_player_idx is not None:
       mask = np.zeros(1262, dtype=bool)
       n = len(state.pending_tutor_matches)
       mask[0:n] = True        # TUTOR_SELECT slots (reinterpretation of PLAY_CARD[0:n])
       mask[1001] = True       # DECLINE_TUTOR (reinterpretation of PASS slot)
       return mask
   ```
2. Confirm the 14.1 pending_post_move_attacker branch runs AFTER (or is otherwise mutually exclusive) so there's no double-write. Add an `assert not (state.pending_tutor_player_idx is not None and state.pending_post_move_attacker_id is not None)`.

3. Document in a comment: "Slots [0:n] and 1001 are reinterpreted while pending_tutor is set. The resolve layer decodes them based on the same pending flag."
  </action>
  <verify>
- `grep -n "pending_tutor_player_idx" src/grid_tactics/legal_actions.py` shows the new branch
- `pytest tests/test_legal_actions.py -q` will be updated in Task 3
  </verify>
  <done>Python legal_actions emits the correct restricted mask in pending_tutor state.</done>
</task>

<task type="auto">
  <name>Task 2: Tensor legal_actions vectorized pending_tutor branch</name>
  <files>src/grid_tactics/tensor_engine/legal_actions.py</files>
  <action>
1. In the batched legal-action computation, after clearing the base mask, add a vectorized pending_tutor branch:
   - `tutor_active = state.pending_tutor_player != -1`  # [N] bool
   - For games where tutor_active: zero out the full mask row, then set `mask[g, 0:n_matches_g]` and `mask[g, 1001]`.
   - Compute `n_matches_g` per game as `(state.pending_tutor_matches[g] != -1).sum(dim=-1)`.
   - Use scatter or a small Python loop over the tutor_active indices (N is small in training; if perf matters, use a scatter approach with per-row lengths).
2. Ensure the 14.1 pending_post_move_attacker branch still runs and is mutually exclusive (assert as in Task 1).
  </action>
  <verify>
- Synthetic test: construct a TensorGameState with 2 of 4 games in pending_tutor, verify mask has exactly the expected True positions
- `pytest tests/test_legal_actions.py -q` will cover this in Task 3
  </verify>
  <done>Tensor legal_actions mirrors Python.</done>
</task>

<task type="auto">
  <name>Task 3: Tests for legal_actions with pending_tutor</name>
  <files>tests/test_legal_actions.py</files>
  <action>
Add tests:

1. `test_legal_actions_pending_tutor_restricts_to_select_and_decline`: construct a state with pending_tutor_matches of length 3. Assert mask has True at exactly slots [0,1,2,1001] and False everywhere else.

2. `test_legal_actions_pending_tutor_length_1`: single match. Only slots [0, 1001] True.

3. `test_legal_actions_pending_tutor_masks_out_play_move_attack_sacrifice_pass`: explicitly check that slots 3..250 (remaining PLAY), 250..350 (MOVE), 350..975 (ATTACK), 975..1000 (SACRIFICE), 1002..1262 (REACT) are all False.

4. `test_legal_actions_no_pending_unchanged`: construct a normal state, assert the mask equals the 14.1-era baseline (use a snapshot or direct comparison to the pre-tutor code path).

5. `test_legal_actions_parity_python_vs_tensor_pending_tutor`: build equivalent Python + tensor states with pending_tutor, assert masks are equal.

6. `test_legal_actions_mutex_assertion`: attempt to construct a state with BOTH pending fields set and call compute_legal_actions — expect AssertionError.

7. `test_legal_actions_pending_post_move_attack_still_works`: regression test covering 14.1 behavior — make sure we didn't break it.
  </action>
  <verify>
- `pytest tests/test_legal_actions.py -q` passes
- `pytest tests/ -q` passes overall (engine + tensor engine + legal actions all green)
  </verify>
  <done>Legal-action masks fully gated on pending_tutor in both engines, parity tested, 14.1 regression covered.</done>
</task>

</tasks>

<verification>
- Full `pytest tests/ -q` green
- `grep -n "pending_tutor" src/grid_tactics/legal_actions.py src/grid_tactics/tensor_engine/legal_actions.py` shows branches in both
</verification>

<success_criteria>
Pending-tutor state produces correctly restricted masks in both engines. 14.1 behavior preserved. Parity verified.
</success_criteria>

<output>
Create `.planning/phases/14.2-tutor-choice-prompt/14.2-03-SUMMARY.md` noting mask reinterpretation, mutex assertion, parity results.
</output>
