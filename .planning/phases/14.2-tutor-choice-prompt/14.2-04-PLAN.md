---
phase: 14.2-tutor-choice-prompt
plan: 04
type: execute
wave: 4
depends_on: ["14.2-01"]
files_modified:
  - src/grid_tactics/server/view_filter.py
  - src/grid_tactics/server/static/game.js
  - src/grid_tactics/server/static/game.css
autonomous: false
must_haves:
  truths:
    - "When a player plays a card that enters pending_tutor, a modal appears showing each matching deck card rendered with FULL CARD ART — the exact same card rendering used in the deck builder / card database view (not a stripped-down tile). Reuse the existing deck-builder card component so art, stats, effects, flavor text, and element/tribe icons all render identically."
    - "Each matched card tile shows 'X of Y copies remaining in deck' where X is copies still in deck (including this one) and Y is total copies the player owned"
    - "A Skip button is visible; clicking it submits DECLINE_TUTOR and dismisses the modal"
    - "Clicking a specific card tile submits TUTOR_SELECT with the correct match index and dismisses the modal"
    - "Deck count remaining is visible in the modal header AND in the existing turn indicator (verified/surfaced if missing)"
    - "After resolve, the UI transitions cleanly; no stale modal remains; react window proceeds"
    - "The old dead-code auto-tutor detection in game.js (the showTutorToast branch checking nextHand.length > prevHand.length + 1) is removed"
  artifacts:
    - path: src/grid_tactics/server/view_filter.py
      provides: "Per-player view serializes pending_tutor_player_idx and pending_tutor_matches (for the caster's own view only — opponent sees 'opponent is choosing' state without card identities)"
    - path: src/grid_tactics/server/static/game.js
      provides: "showTutorModal(matches, deckSize, totalCopiesByCardId) renderer; click handlers submit TUTOR_SELECT/DECLINE_TUTOR actions; dead auto-tutor code removed"
    - path: src/grid_tactics/server/static/game.css
      provides: "Modal styles: overlay, card fan layout, skip button, copy-count pill, responsive desktop+mobile"
  key_links:
    - from: src/grid_tactics/server/static/game.js::handleStateUpdate
      to: src/grid_tactics/server/static/game.js::showTutorModal
      via: "When incoming state has pending_tutor_player_idx === my_player_idx, call showTutorModal(state.pending_tutor_matches_resolved)"
      pattern: "pending_tutor_player_idx"
    - from: src/grid_tactics/server/view_filter.py
      to: src/grid_tactics/server/static/game.js
      via: "Resolves deck indices to full card definitions before sending (client doesn't know deck order); opponent view shows only match count"
      pattern: "pending_tutor"
---

<objective>
Ship the player-facing selection modal. Serialize pending_tutor to the client (asymmetric per-player view), render the modal, wire submit handlers, and remove the dead auto-tutor detection code.

Purpose: Close the user-visible gap. This is the whole point of Phase 14.2.
Output: view_filter serialization, game.js modal + handlers, game.css styles, manual verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14.2-tutor-choice-prompt/14.2-01-PLAN.md
@.planning/phases/14.1-melee-move-and-attack/14.1-04-SUMMARY.md
@src/grid_tactics/server/view_filter.py
@src/grid_tactics/server/static/game.js
@src/grid_tactics/server/static/game.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialize pending_tutor in view_filter (asymmetric)</name>
  <files>src/grid_tactics/server/view_filter.py</files>
  <action>
1. In the per-player state serializer, after serializing `pending_post_move_attacker_id`, add pending_tutor serialization:
   - Always include `pending_tutor_player_idx` (so any client knows if tutor is in progress and whose turn-to-pick it is).
   - Always include `deck_size` for both players (verify if already present — the must-haves require deck count visibility; if missing in the current serializer, ADD it).
   - If `pending_tutor_player_idx == viewer_player_idx`: include `pending_tutor_matches` as a list of **resolved card objects** (full card definition + the absolute deck index). The client renders these directly.
   - If `pending_tutor_player_idx != viewer_player_idx`: include only `pending_tutor_match_count: int` (opponent sees "N cards being considered") but NOT the card identities.
   - Also include `pending_tutor_total_copies_owned: Dict[card_id, int]` for the caster — count of how many copies of each matching card the caster originally has across their WHOLE card pool (deck + hand + board), so the modal can show "2 of 3 copies remaining in deck".

2. Also surface `deck_size` in the top-level turn indicator payload if not already. The must-have requires it visible outside the tutor flow.
  </action>
  <verify>
- Start dev server, play a tutor card, inspect websocket payload (browser devtools): confirm the caster's state has `pending_tutor_matches` populated with resolved cards, opponent's state has only `pending_tutor_match_count`
- Confirm `deck_size` appears in normal (non-tutor) state payloads
  </verify>
  <done>Serializer sends asymmetric per-player pending_tutor data plus deck_size.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend modal — render, styles, submit handlers, remove dead code</name>
  <files>src/grid_tactics/server/static/game.js, src/grid_tactics/server/static/game.css</files>
  <action>
1. `game.js`: find and DELETE the dead auto-tutor detection around lines 1458-1475 (the `showTutorToast` branch checking `nextHand.length > prevHand.length + 1`). That code path has never fired for tutor-on-play and is now obsolete.

2. Add `showTutorModal(matches, deckSize, totalCopiesByCardId)`:
   - Inputs: `matches` is an array of `{card: CardDef, deck_idx: int, match_idx: int}`; `deckSize` is current deck card count; `totalCopiesByCardId` maps card_id -> how many copies the caster originally owned.
   - Render a full-screen overlay (`.tutor-modal-overlay`) with a centered panel (`.tutor-modal`).
   - Header: "Choose a card to tutor" + "Deck: {deckSize} cards remaining".
   - Card fan: map `matches` to card tiles using the existing `renderCard(cardDef)` helper (reuse deck-builder card renderer for full art+stats).
   - Each tile has a copy-count pill: compute `remainingInDeck` = number of times `card.card_id` appears among `matches` (since matches come from deck, that's the deck-remaining-including-this-one); `totalOwned` = `totalCopiesByCardId[card.card_id]`. Render as "`{remainingInDeck} of {totalOwned} copies remaining`".
   - Click on a card tile -> submit action `{type: "TUTOR_SELECT", card_index_in_deck: match.match_idx}` via the existing websocket action sender.
   - Skip button -> submit `{type: "DECLINE_TUTOR"}`.
   - Dismiss the modal in the next state-update when `pending_tutor_player_idx !== my_idx`.

3. In `handleStateUpdate` (or equivalent), after existing pending_post_move_attack handling, add:
   ```js
   if (state.pending_tutor_player_idx === myPlayerIdx && !tutorModalOpen) {
       showTutorModal(state.pending_tutor_matches, state.deck_size_self, state.pending_tutor_total_copies_owned);
   } else if (state.pending_tutor_player_idx !== myPlayerIdx && tutorModalOpen) {
       closeTutorModal();
   }
   ```
   If opponent is choosing (pending set but not mine), show a small non-blocking toast "Opponent is tutoring…" but no modal.

4. Surface `deck_size_self` in the turn indicator element if not already visible. Keep it simple: `"Deck: {n}"` next to the mana/HP displays.

5. `game.css`: add styles for `.tutor-modal-overlay` (fixed, full-viewport, semi-transparent backdrop, z-index above board), `.tutor-modal` (centered panel, max-width 900px, rounded, shadow), `.tutor-modal-header`, `.tutor-modal-cards` (flex row, gap, wrap on mobile), `.tutor-copy-count` (pill), `.tutor-skip-button` (prominent, secondary color). Ensure responsive breakpoint for mobile (stack cards vertically).
  </action>
  <verify>
- Dead auto-tutor code removed: `grep -n "nextHand.length > prevHand.length" src/grid_tactics/server/static/game.js` returns no matches
- Modal renders: manually played in next task
  </verify>
  <done>Modal implemented, dead code removed, deck count surfaced, styles in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual end-to-end verification</name>
  <what-built>
Full tutor-choice-prompt flow: Python engine pending state (Wave 1), tensor parity (Wave 2), legal-action masks (Wave 3), and the frontend modal + serialization (this wave).
  </what-built>
  <how-to-verify>
1. Start the dev server: `python -m grid_tactics.server` (or the project's usual dev command).
2. Open two browser tabs at `http://localhost:{PORT}`, join the same room as Player 1 and Player 2.
3. **Happy path — pick a card:**
   - P1 builds a deck containing Blue Diodebot and at least 2 Red Diodebots.
   - P1 plays Blue Diodebot.
   - Expected: a modal appears for P1 showing both Red Diodebots with full art, each tagged "1 of 2 copies remaining in deck" (or similar), deck count visible in header.
   - P2's screen shows a small toast "Opponent is tutoring…" and the board is locked (cannot act).
   - P1 clicks one Red Diodebot tile.
   - Expected: modal closes, chosen card appears in P1 hand, deck size decremented, react window offered to P2 (once, not twice).

4. **Decline path:**
   - P1 plays another tutor card into a deck with matches.
   - Click Skip.
   - Expected: modal closes, nothing added to hand, deck unchanged, react window fires once.

5. **No-matches path:**
   - P1 plays Blue Diodebot with an empty deck of Red Diodebots.
   - Expected: no modal appears, action resolves as a normal on_play (react window fires normally).

6. **14.1 regression:**
   - Play a melee minion and move it adjacent to an enemy.
   - Expected: the post-move attack-pick highlights still work (14.1 behavior preserved, unaffected by tutor changes).

7. **Deck count visibility:**
   - Confirm the turn indicator shows "Deck: N" even outside the tutor flow, for both players (each sees their own).

8. **Mobile check:**
   - Resize browser to mobile width. Confirm modal cards stack vertically and Skip button is reachable.
  </how-to-verify>
  <resume-signal>
Type "approved" if all six paths work. If anything is broken, describe exactly which step failed and what you saw.
  </resume-signal>
</task>

</tasks>

<verification>
- Manual checkpoint passes
- No JS console errors during any flow
- Websocket payloads contain pending_tutor fields as designed
</verification>

<success_criteria>
Modal appears, fans matched cards with full art, shows copy counts + deck size, player can pick or decline, 14.1 is unaffected, dead code is gone.
</success_criteria>

<output>
Create `.planning/phases/14.2-tutor-choice-prompt/14.2-04-SUMMARY.md` noting UI decisions (asymmetric view, toast for opponent, mobile layout).
</output>
