---
phase: 14.2-tutor-choice-prompt
plan: 05
type: execute
wave: 5
depends_on: ["14.2-03", "14.2-04"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
must_haves:
  truths:
    - "Roadmap reflects Phase 14.2 inserted after 14.1 in v1.1 milestone with status complete"
    - "STATE.md records tutor_target selector schema and pending_tutor mechanic as project decisions"
    - "STATE.md re-notes RL checkpoint staleness (now cumulative with 14.1) as a known non-blocking issue"
    - "End-to-end smoke on the dev server confirms pick, decline, no-match, and 14.1 regression all pass"
    - "Decline-allowed default is recorded as tunable if balance testing later suggests forcing a pick"
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "Phase 14.2 entry marked complete with plan list"
    - path: .planning/STATE.md
      provides: "Decision entries for tutor selector schema, pending_tutor mechanic, decline-allowed default, RL stale-checkpoint note"
  key_links: []
---

<objective>
Land the phase: update planning docs, run a final smoke test, commit.

Purpose: Close the phase formally so downstream work (15 reconnection) starts from a clean state.
Output: Updated ROADMAP.md + STATE.md, smoke test results recorded.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2-tutor-choice-prompt/14.2-01-SUMMARY.md
@.planning/phases/14.2-tutor-choice-prompt/14.2-02-SUMMARY.md
@.planning/phases/14.2-tutor-choice-prompt/14.2-03-SUMMARY.md
@.planning/phases/14.2-tutor-choice-prompt/14.2-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md and STATE.md</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
1. `ROADMAP.md`: locate the v1.1 milestone section. Confirm Phase 14.2 entry exists (the planner created it). Update:
   - Status: complete
   - Goal: one-line summary ("Replace auto-tutor with player-facing selection modal; extend tutor_target to support selector dicts")
   - Plans: list all 5 plans as checked (`- [x]`):
     ```
     - [x] 14.2-01-PLAN.md — Python engine pending_tutor + tutor_target selector schema
     - [x] 14.2-02-PLAN.md — Tensor engine parity
     - [x] 14.2-03-PLAN.md — Legal-action masks
     - [x] 14.2-04-PLAN.md — Frontend modal + serialization
     - [x] 14.2-05-PLAN.md — Roadmap/STATE + smoke test
     ```
   - Ensure ordering: 14 -> 14.1 -> 14.2 -> 15.

2. `STATE.md`: append to Decisions:
   - "14.2: tutor_target accepts either a card_id string OR a selector dict with tribe/element/card_type keys (AND semantics)."
   - "14.2: tutor on_play enters a pending_tutor state; player picks from a modal or declines. Exactly one react window fires after resolve."
   - "14.2: decline-allowed is the default behavior; tunable if balance testing later suggests forcing a pick."
   - "14.2: action-space [0:1262] preserved; TUTOR_SELECT reuses PLAY_CARD[0:K] slots and DECLINE_TUTOR reuses slot 1001 while pending_tutor is set."
   - Update known-issues: "RL checkpoints are stale after 14.1 + 14.2 encoding reinterpretations. Loadable but behaviorally outdated. Retraining deferred."
   - Update current position to "Phase 14.2 complete; next: Phase 15 reconnection."
  </action>
  <verify>
- `grep -n "14.2" .planning/ROADMAP.md` shows the phase section with all 5 plans checked
- `grep -n "tutor_target" .planning/STATE.md` shows the new decisions
  </verify>
  <done>Planning docs updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final smoke test</name>
  <what-built>All Phase 14.2 work across 5 plans.</what-built>
  <how-to-verify>
1. `pytest tests/ -q` — full suite green.
2. Start dev server, run two-tab game:
   - Pick path: play Blue Diodebot with multiple Red Diodebots in deck -> modal -> click one -> card in hand -> one react window.
   - Decline path: same setup -> Skip -> one react window, deck unchanged.
   - No-match path: play tutor card with empty deck of matches -> no modal -> action ends normally.
   - 14.1 regression: melee minion move+attack still highlights tiles and fires one react.
3. Confirm deck count is visible in turn indicator outside the tutor flow.
4. Check browser console: no errors during any path.
  </how-to-verify>
  <resume-signal>Type "approved" if all four paths + pytest pass. Otherwise describe what broke.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Commit the planning updates plus any SUMMARY files generated by earlier waves that haven't been committed yet. Suggested message:

```
docs(14.2): complete tutor-choice-prompt phase

- Python engine: pending_tutor state, selector dict tutor_target
- Tensor engine: parity with K=8 match slots
- Legal actions: slot reinterpretation while pending_tutor
- Frontend: selection modal with full card fan, copy counts, skip
- Action space [0:1262] preserved
- 14.1 regression clean
```
  </action>
  <verify>`git log -1 --oneline` shows the commit</verify>
  <done>Phase 14.2 landed.</done>
</task>

</tasks>

<verification>
- Roadmap + STATE updated
- Full pytest suite green
- Smoke test paths all pass
- Commit on master
</verification>

<success_criteria>
Phase 14.2 is complete, documented, committed, and verified end-to-end. Ready to move on to Phase 15.
</success_criteria>

<output>
Create `.planning/phases/14.2-tutor-choice-prompt/14.2-05-SUMMARY.md` noting final smoke test results and any follow-up items surfaced during verification.
</output>
