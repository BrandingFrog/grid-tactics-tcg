---
phase: 14.3-game-juice
plan: 07
wave: 7
subsystem: client-ui
tags: [game-juice, popups, status-badges, floating-text, burning, frontend]
completed: 2026-04-07
duration: ~8min
requires: [14.3-01, 14.3-04, 14.3-06]
provides:
  - showFloatingPopup(tileEl, text, variant) — reusable 5-variant popup
  - collectMinions(state) / getTileElForMinion(m) helpers
  - Heal + burn-tick diff hooks in applyStateFrame
  - Combat-damage popup routed through showFloatingPopup (replaces Wave 4 inline)
  - Persistent 🔥 / ⬆️+N / ⬇️-N badges in renderBoardMinion
  - Luckiest Guy font load (popups only)
affects:
  - Future status effects (frozen, stunned, poisoned) reuse this popup pathway
  - Wave 5 closeout still owes a STATE.md/ROADMAP.md update reflecting 7 plans
key-files:
  created: []
  modified:
    - src/grid_tactics/server/static/game.html
    - src/grid_tactics/server/static/game.css
    - src/grid_tactics/server/static/game.js
decisions:
  - "showFloatingPopup is the single popup pathway. Wave 4's inline .damage-popup is replaced; the old .damage-popup CSS block is left in place (harmless)."
  - "Heal + burn-tick hooks live in applyStateFrame BEFORE the gameState mutation, so prev-vs-next minion HP deltas can be diffed by instance_id."
  - "Burn-tick popup anchors to the PREV tile (via getTileElForMinion(p)) so lethal burns still show the number before the minion is removed by the upcoming renderBoard."
  - "Burn-tick gate: turnFlipped (prev.active_player_idx !== next.active_player_idx) AND p.burning_stacks > 0 AND nextHp < prevHp. This matches the engine's tick semantics from 14.3-06 (tick fires once per turn flip, after react resolves)."
  - "Heal popup gate: any current_health increase, any frame. Delta = nextHp - prevHp."
  - "Badges are rendered as innerHTML inside renderBoardMinion (matching its existing string-build style), not appended to tileEl post-hoc. This keeps the badges inside the .board-minion node so they survive renderBoard re-renders without extra DOM tracking."
  - "Combat damage and burn damage cannot collide visually: combat damage flows through playAttackAnimation (whose stateAfter is the post-attack frame) and burn-tick flows from a different state frame (the turn-flip frame after react resolution)."
  - "Luckiest Guy is loaded via Google Fonts <link> in <head> and applied ONLY in .floating-popup. body font is unchanged."
  - "Diff helpers (collectMinions, getTileElForMinion) are tiny but named so future statuses can reuse them without inlining querySelector strings."
  - "Task 4 visual verification deferred to post-deploy Playwright E2E against Railway (same posture as 14.1-04 / 14.2-04 / 14.3-01 / 14.3-04)."
metrics:
  duration: ~8min
  completed: 2026-04-07
  tasks: 3
  commits: 3
  files_changed: 3
---

# Phase 14.3 Plan 07: Floating Popups + Status Badges Summary

**One-liner:** Reusable `showFloatingPopup` (5 variants — combat-damage / heal / burn-tick / buff / debuff) renders Luckiest-Guy floating text from prev/next state diffs in `applyStateFrame`, and `renderBoardMinion` now shows persistent 🔥 / ⬆️+N / ⬇️-N corner badges driven by `burning_stacks` and `attack_bonus`.

## API Surface

### `showFloatingPopup(tileEl, text, variant)`

| Variant | Color | Glyph convention |
|---|---|---|
| `combat-damage` | red `#ff3030`   | `⚔️ -X` |
| `heal`          | green `#3aff70` | `💚 +X` |
| `burn-tick`     | orange `#ff8030`| `🔥 -X` |
| `buff`          | cyan `#30dfff`  | `⬆️ +X ATK` |
| `debuff`        | purple `#c060ff`| `⬇️ -X ATK` |

`tileEl` should be a `.board-cell` (already `position: relative`). Self-removes via `animationend` listener with a 1400ms `setTimeout` fallback. Animation = `popup-rise` 1200ms (rise 72px + fade).

### Hook locations

| Where | Triggers |
|---|---|
| `playAttackAnimation` PHASE D (game.js) | combat-damage popup, anchored to target cell, uses `last_action.damage` from view payload |
| `applyStateFrame` (game.js, before `gameState = frame`) | heal popup on any HP increase; burn-tick popup when `prev.active_player_idx !== next.active_player_idx` AND `prev.burning_stacks > 0` AND HP decreased |
| `renderBoardMinion` (game.js) | persistent badges in `.minion-badges` container (bottom-left corner of cell) |

### Helpers

- `collectMinions(state)` → flat array of board minions (just returns `state.minions || []`).
- `getTileElForMinion(m)` → `.board-cell` DOM node at `m.position[0], m.position[1]`, or null.

## Recipe: Adding a New Status Popup (e.g. frozen)

1. **CSS**: Append one variant rule to `.floating-popup.frozen { color: #80c0ff; }` in game.css.
2. **Diff hook**: In `applyStateFrame`, add a per-minion check (e.g. `if (m.frozen_stacks > p.frozen_stacks) showFloatingPopup(tileEl, '❄️ +1', 'frozen')`).
3. **Badge** (optional): Append to the `badges` array in `renderBoardMinion` with a new `.badge-frozen` CSS class.

That's it. No queue, animation infra, or render surgery needed.

## Tasks Completed

1. **Task 1** (`faf8e10`) — `game.html` Google Fonts link; `game.css` floating-popup base + 5 variants + popup-rise keyframe + minion-badges + badge classes.
2. **Task 2** (`ff35d91`) — `showFloatingPopup`, `collectMinions`, `getTileElForMinion` in game.js; `renderBoardMinion` emits `.minion-badges` from `burning_stacks` + `attack_bonus`.
3. **Task 3** (`2cac8e2`) — `playAttackAnimation` swaps inline `.damage-popup` for `showFloatingPopup('combat-damage')`; `applyStateFrame` adds prev/next minion HP diff for heal + burn-tick popups (with turn-flip gate for burn).

## Deviations from Plan

None of substance. Two minor implementation choices:

1. **Badges as string-built innerHTML** (not `createElement` + `appendChild` to tileEl) — matches `renderBoardMinion`'s existing style, which returns an HTML string consumed by `cell.innerHTML = renderBoardMinion(minion)`. Appending after-the-fact would be wiped on every renderBoard.
2. **Burn-tick anchor to PREV tile** via `getTileElForMinion(p)` (with `tileEl` fallback) — guarantees lethal burns still show the popup before the minion is removed when `renderGame()` runs at the bottom of `applyStateFrame`. Plan suggested either approach; chose the safer one.

No Rule 1/2/3 fixes needed. No Rule 4 architectural decisions.

## Authentication Gates

None.

## Verification

- `node -e "new Function(...)"` parse-check on game.js — OK.
- pytest deferred (local env still missing `stable_baselines3`; this wave touches only client files, no Python).
- Task 4 manual / Playwright E2E deferred to post-deploy verification against Railway (same posture as prior bug-fix and animation waves).

## Next Phase Readiness

Phase 14.3 game-juice arc is now feature-complete for combat, summon, move, attack, burning, popups, and badges. Wave 5 closeout still owes a STATE.md/ROADMAP.md amendment to reflect that Phase 14.3 grew from the planned 5 plans to 7 (waves 6 burning + 7 popups added mid-phase). Future status effects (frozen, stunned, poisoned, blessed) follow the engine pattern from Wave 6 + the visual pattern documented above.

## Commits

- `faf8e10` feat(14.3-07): Luckiest Guy font + floating popup CSS + status badges
- `ff35d91` feat(14.3-07): showFloatingPopup() + helpers + renderBoardMinion badges
- `2cac8e2` feat(14.3-07): wire combat/heal/burn-tick popups via showFloatingPopup
