---
phase: 14.1-melee-move-and-attack
plan: 05
type: execute
wave: 5
depends_on: ["14.1-03", "14.1-04"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
must_haves:
  truths:
    - "Roadmap reflects Phase 14.1 as inserted between 14 and 15 in the v1.1 milestone with status complete"
    - "STATE.md records the new mechanic as a project decision and notes RL checkpoint staleness as a known non-blocking issue"
    - "An end-to-end smoke test on the dev server confirms melee chain, ranged no-chain, and decline path all work in real gameplay"
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "Phase 14.1 entry, status, plan list"
    - path: .planning/STATE.md
      provides: "Decision: 'Melee minions chain move+attack; ranged do not.' + known issue: 'RL checkpoints pre-14.1 are stale (learned auto-attack); functional but suboptimal until retrain.'"
  key_links: []
---

<objective>
Close out Phase 14.1: update planning docs, document the RL checkpoint staleness as a known issue, and run a final integration smoke test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.1-melee-move-and-attack/14.1-01-SUMMARY.md
@.planning/phases/14.1-melee-move-and-attack/14.1-02-SUMMARY.md
@.planning/phases/14.1-melee-move-and-attack/14.1-03-SUMMARY.md
@.planning/phases/14.1-melee-move-and-attack/14.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md and STATE.md</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
1. In `.planning/ROADMAP.md`, insert Phase 14.1 between Phase 14 and Phase 15 in the v1.1 milestone:
   ```
   ### Phase 14.1: Melee move-and-attack
   **Goal:** Replace auto-attack-after-move with player-driven Advance-Wars-style melee chain (move then choose attack target or decline). Ranged minions get no chain.
   **Plans:** 5 plans
   Plans:
   - [x] 14.1-01-PLAN.md — Python engine pending state + remove auto-attack
   - [x] 14.1-02-PLAN.md — Tensor engine parity
   - [x] 14.1-03-PLAN.md — Legal-action mask pending awareness
   - [x] 14.1-04-PLAN.md — Frontend two-step flow + range footprint
   - [x] 14.1-05-PLAN.md — Roadmap/state update + smoke test
   ```

2. In `.planning/STATE.md`:
   - Add to Decisions: `2026-04-07: Melee minions (attack_range == 0) chain move+attack as one action via post-move pending state. Ranged minions do not chain. One react window per logical action. Action-space layout [0:1262] preserved; slot 1001 reused as DECLINE_POST_MOVE_ATTACK when pending.`
   - Add to Known Issues / Concerns: `RL checkpoints trained before Phase 14.1 are STALE — they learned the old auto-attack-after-move behavior. Loadable but suboptimal. Retrain plan deferred to a future phase; not blocking gameplay.`
   - Update Current Position to point to whatever comes after 14.1.
  </action>
  <verify>`grep -n "14.1" .planning/ROADMAP.md` shows the new entry; `grep -n "14.1" .planning/STATE.md` shows the decision</verify>
  <done>Both planning docs updated.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite + tensor parity sanity check</name>
  <files>(no file edits)</files>
  <action>
Run:
1. `pytest tests/ -q` — full suite must pass.
2. `pytest tests/ -q -k "parity"` — parity tests green.
3. `grep -rn "_auto_attack_after_move" src/` — must return zero matches.
4. `grep -rn "pending_post_move_attacker" src/` — must show usages in action_resolver, game_state, tensor_engine/state, tensor_engine/actions, legal_actions, tensor_engine/legal_actions, and serializers.
  </action>
  <verify>All four checks pass.</verify>
  <done>Codebase is clean of auto-attack and consistent on pending-state references.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end smoke test on dev server</name>
  <what-built>Complete Phase 14.1 stack: engine + tensor + legal actions + UI.</what-built>
  <how-to-verify>
1. Start dev server, two-tab game.
2. Play a full short game exercising:
   - At least one melee move + attack chain (verify combat happens, one react window)
   - At least one melee move + decline (verify no combat, one react window)
   - At least one ranged move alone (verify no attack-pick, react fires immediately)
   - At least one ranged attack as a separate action (verify still works as today)
3. Confirm no crashes, no double react windows, no stuck pending state.
4. Confirm game log reads naturally for the new flow (move line, then either attack line or "declined attack" line).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue.</resume-signal>
</task>

</tasks>

<verification>
Full test suite green. Roadmap + STATE updated. Smoke test approved.
</verification>

<success_criteria>
Phase 14.1 is fully shipped: engine, tensor, mask, UI, and docs all updated and verified by the user.
</success_criteria>

<output>
Create `.planning/phases/14.1-melee-move-and-attack/14.1-05-SUMMARY.md` and a phase-level closeout note.
</output>
