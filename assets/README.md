# Source artwork

`art/` contains source card illustrations used by asset-generation tooling and documentation. The browser-served copies live under `src/grid_tactics/server/static/art/`.

Keep filenames stable because card metadata and generation scripts may refer to them. Optimize large additions before committing them.
