# Analytics dashboard

This is a separately deployed static dashboard for training analytics.

- `index.html` contains the Supabase and Chart.js client.
- `supabase_schema.sql` documents the required database schema.
- `vercel.json` routes dashboard paths to the single-page application.

It is not part of the Railway PvP server deployment.
